chore: 撤出误入库的审计临时目录(17.7MB/55 文件),并补忽略规则

上一个提交(6a197e5)我用 `git add -A` 时把工作区里**别人正在用**的临时目录一起带进去了:
- `.audit-tmp/`(55 个文件、17.7 MB,含 Playwright 的 node_modules 与审计脚本)
- `.trae-html-share-packages`

它们不是要入库的东西,现在 `git rm --cached` 撤出(**磁盘文件保留**,别人还在用),
并加忽略规则,免得下次再被 `-A` 扫进来。其余同批进来的文件是另一位 agent 的正式产出
(对接指南/实施规格/hc_* 工具),保留入库。

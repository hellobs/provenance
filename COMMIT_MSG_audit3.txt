fix(case01): 废 Run 出服务目录 + 调试跑打标(第三份清单核查)

逐条核了第三份清单的 4 条:1 条不成立(当前 HEAD 就能编译)、2 条**已经修过**
(清单是旧状态)、1 条属实且根因比它说的更具体。

- 问题 1(live_switch.py 缩进):**当前 HEAD 无此问题** —— `py_compile` 通过,
  case01 316 条全绿。大概率是另一个 AI 在我**改那个文件的同一时刻**读到了中间态
  (我加 judge 命名注释时那一行短暂处于半改状态)。已在报告里说明。
- 问题 3(B-1613 缺 risk_note/field):**已修**(本轮早先的 `bafd67e`)。
  根因不是"旧数据",是解析器只认 `field`/`risk`,而模型输出的是 `required_expert`/`risk_level`;
  加别名 + `--router-only` 重跑后,B-1613 五条字段齐全。
- 问题 4(B-1654 反思以问句结尾):**已修**(同上轮);现在结尾是正常陈述句。
- 问题 2(B-1652/B-1654 是废跑):属实,但根因不是"模型把 final feedback 当 T0 输入",
  而是 **`--nodes 1` 截断**:截断后 node-1 **同时是末节点**,`_apply_world` 于是按"末节点"
  注入最终反馈语境 → Ethan 在 T0 张口就是"我没买 HCM…"。这是我自测重开功能时留下的。

本次修法(把"调试跑"变成一等公民,不再靠人记):
- `MavisBridge(debug_note=...)` → 原始记录与成品记录都带 `debug` 字段;
  `live_run --nodes N` 一旦截断就打印并写入说明(实测 1 节点跑必然如此)。
- `quality_of()`:`debug` 优先于一致性判定 → `quality="debug"`;
  `serve.py` 的 `/api/runs` **默认同时排除 questionable 与 debug**,并在 `excluded`
  里按类别报出 run_id 与原因(不静默),`?include_questionable=1` 取全量。
- B-1652/B-1654(连同其原始记录)移出 `case01/runs/` 到归档。
  真机验证(5002 重启后):默认返回 14 条、`excluded.by_kind.questionable=[A-1720]`、全量 15 条。

测试:case01 317 / vizkit 27 / 外层 28。

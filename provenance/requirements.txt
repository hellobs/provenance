# Provenance 平台运行依赖(Python 3.12)
# 框架:mavisframework(独立仓库 hellobs/mavis)
#   >=1.2.0 起含 case01 需要的全部扩展点:三处注入钩子(external_state /
#   interaction_request / role_directive)与通用插件面(mavisframework.plugin、
#   Simulator(plugins=)、agent_core.subscribe_chat_line)。
#   1.1.0 的制品不含插件面;1.0.0 连钩子都不含,装了会在构造 Simulator 时
#   TypeError。所以下界必须是 1.2.0。
#   1.2.1 另修了 _resolve_assets_root 对绝对路径的破坏(吃掉 POSIX 前导斜杠、
#   把 "D:/x" 拼成驱动器相对路径 "D:x")。case01 传的是相对根,用不到这个修复,
#   故下界仍留 1.2.0 而不是 1.2.1——抬高下界只会让装有 1.2.0 的环境无谓失败。
#   传绝对 assets_root 的使用方(含用 MAVIS_ASSETS_ROOT 环境变量)请装 >=1.3.0。
#   本仓不发布到 PyPI:先在 mavis 仓构建 wheel 再安装
#   (mavis main 的 1.3.0 提交为 a7f56c5,tag v1.3.0(上一版 1.2.1 为 1516ada);dist/ 不入库,需自行 python -m build)。
# 写法用"下界 1.2.0 + 上界 <2.0.0":CI 以 git clone --depth 1 ... main 装框架,
# main 是移动目标,写死 == 会在 mavis 每次次版本 bump 时把 CI 误打红(那种红不代表缺陷);
# "基线至少含插件面"这件事由 tools/check_engine_baseline.py 的能力断言独立保证。
mavisframework>=1.2.0,<2.0.0

# 可视化插件集(独立包,见 packages/mavis-vizkit;editable 安装,只消费事件字典契约)
# 本仓不发布到 PyPI:先 pip install -e packages/mavis-vizkit(CI 亦如此)。
mavis-vizkit==0.1.0

# 注入器包(独立包,见 packages/mavis-case01-injector;阶段 3 抽包,editable 安装)
# 本仓不发布到 PyPI:先 pip install -e packages/mavis-case01-injector(CI 亦如此)。
mavis-case01-injector==0.1.0

# 实时服务(FastAPI + WebSocket + 模板渲染)
fastapi>=0.110
uvicorn>=0.29
websockets>=12.0
jinja2>=3.1

# 测试客户端:fastapi.testclient 依赖 starlette.testclient,新版本(随 fastapi 解析到)
# 要求 httpx2(优先)或 httpx。CI 单源从本文件安装时若缺它,用 TestClient 的测试会
# 报 RuntimeError("requires httpx2")。本行必须保留。
httpx2>=0.28

# 导出倾向曲线 PNG(export-chart 接口,matplotlib 渲染)
matplotlib>=3.8

# OpenAPI 漂移守卫测试(case01/tests/test_serve.py 在函数体内 import yaml,
# 读 docs/case01_api.openapi.yaml 做"静态契约 vs 实际路由/枚举"对齐断言)。
# 本行必须保留:CI 从本文件单源安装依赖,缺它守卫测试在 CI 必红(ModuleNotFoundError)。
pyyaml>=6.0

# 可选:向量记忆检索(默认 SimpleStore 纯 stdlib 不需要;要用向量才装)
# llama-index>=0.10

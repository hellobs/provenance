# 0924 下一步方向(给实现侧)

> **状态**:现行
> **最后核对**:2026-09-24
> **说明**:方向与派单:先打通研究闭环,再补内化,最后收工程底座(每条含验收)
> 依据 28 条真实记录的实测(`0924_IVD符合性与对接就绪度.md`);
> 每条含**现状 / 做什么 / 验收 / 依赖**。口径问题标"待研究侧拍",流程问题标"待平台侧确认"。
> 归属:实现侧 = 写代码;研究侧 = 定口径与资料;平台侧 = 治理工作台。

## 一句话

机制面已经够用(五条 IVD 不变量里四条实测成立),现在**卡在"闭环"上**:
判定语料只有六成走 agent 判断、"内化"这一环在 case01 没有载体、
平台侧拿到 `issue.field` 却建不了专家池。
所以下一步顺序是:**先把研究闭环打通 → 再补内化 → 最后收工程底座**,不要反过来先做界面。

## 方向 A:让"决策由 agent 产生"在语料上成立(P0,最该先做)

**现状**:`branch_source` 实测 `judge` 16 / `preset` 12 —— 43% 的记录里分支是运行参数给的,
这批语料本身不构成"决策由 agent 产生"的证据。守卫有"同场景两种回答 → 两个分支"的反事实测试,
但**没有一条真实记录**这样做过对照。

**做什么**
1. 用默认 `judge` 模式跑够样本(建议 ≥ 10 局),`preset` 只作对照条件;
2. 做**反事实对**:同一场景、同一时间线,喂两种显著不同的 T0 回答,记录成对保存并在索引里可辨;
3. 索引里把 `preset` 显式标出来(字段已有 `branch_source`,面板/契约里也要能一眼看出)。

**验收**:10 局里 `judge` 占比 ≥ 90%;≥3 组反事实对,两局分支不同且 `judge_info.reason` 可见;
`/api/runs` 能按 `branch_source` 过滤。
**依赖**:无(纯实现侧)。

## 方向 B:补 IVD 的"内化"环(P0,口径待研究侧拍)

**现状**:case01 记录 **28/28 零 `tendency`**;`state_history` 只有对手方资金/持仓。
价值倾向只在 case00(已冻结)那条线。也就是说"决策→后果→反思→**内化**"的最后一环,
在现在这条主线上**没有载体**。对外讲 IVD 时这是个会被问到的点。

**两条路,择一(研究侧拍)**
- **B1 把内化做进 case01**:最小可行 = 后果(盈亏、是否与 AI 的 T0 判断一致)写进内核倾向,
  且**下一局的检索与措辞受它影响**。关键:倾向归内核(mavis),案例侧只传后果,不直接改倾向。
- **B2 明确由 case00 承载**:case01 定位为"判断与后果",对外叙事按此切分,
  所有材料不再暗示 case01 有倾向内化。

**验收**:B1 → 同一角色连跑两局,第二局的检索/措辞能被第一局后果影响,且记录里查得到这条影响;
B2 → 文档、对接材料、演示口径统一,字面上不再出现"case01 展示内化"这类说法。
**依赖**:研究侧定"后果→倾向"的换算口径;B1 需实现侧改内核(只允许加法、默认关闭)。

## 方向 C:打通研究闭环(受控实验 → 专家审核 → 金标 → 训练材料)(P0,阻塞对接)

**现状**:平台侧**能建单**(123 条 issue 字段齐,只有 4 条缺 `routing_reason`),
但**建不了池**、**写不回**、**复算不了**、**没有统计出口**。

**做什么**
1. **`issue.field` 受控词表**(硬卡点):实测 123 条 issue 用了 **115 个不同取值**。
   定一份固定标签(建议 8–12 个),提示词里约束、落盘时校验,历史值给映射表或标 `legacy`;
   产出《字段标签清单》交给平台侧建池。
2. **写回鉴权**:平台侧若要专家改约束/标记反思,先加一层(反代 basic auth 或平台侧签名)+ 审计留痕。
3. **导出接口**:一条命令导出"标注包"(issue + 反思原文 + 判定留痕 + 该局 manifest),
   供人工金标与训练材料使用。
4. **N 局批量与统计**:同条件跑 N 局,输出分支分布、判定稳定性、问题条数、耗时。
5. **金标工具**:20–30 条判定与反思的人工标注入口与一致性计算。

**验收**:平台侧用标签清单能建池;导出包能被标注人直接读;`一条命令` 给出 N 局统计;
金标给出**准确率与同输入一致性数字**。
**依赖**:①词表口径(研究侧)②专家池字段(平台侧确认)③金标(研究侧标注)。

## 方向 D:可复算与可信(P1)

**现状**:`manifest`(git commit / 运行方式 / 判定后端与模型 / 温度 / 各哈希)**只有 1/28**;
判定重试留痕(`attempts`/`raw_outputs`)在记录里 **0/28** —— 代码有、真实记录没验证过。

**做什么**:①新跑的一律带 manifest;②历史 27 条**标注"不可复算"**(别让平台以为条条能复算);
③口径/提示词/资料变更写变更日志;④跑一局真实记录,确认 `attempts`/`raw_outputs` 进 `run.json`;
⑤可选:定时重跑同一场景做漂移检测。

**验收**:`/api/runs` 能区分"有清单/无清单";新记录 `judge_info` 含重试与原始输出;
任意一条新记录能按 manifest 复算。
**依赖**:无。

## 方向 E:工程底座(P1–P2)

1. **四个面的起停与看护**:现在 5010 靠 `live_switch`,只读面(5002/5003/8060)靠手工 ——
   这轮就被工具会话崩溃带走了一次。做一键起停 + 看门狗(被杀自动重起并留日志)。
2. **可观测性**:288 处 `print` vs 8 处 logger;时间戳无时区;缺每局指标(耗时/token/失败率)。
3. **CI 守卫继续加**:`case_engine/tests` 已进 CI(实测 182 passed);
   接下来把"写端点清单↔代码""字段词表""夹具存在性"都做成守卫(本次已加两条,见下)。
4. **依赖锁定**:`requirements.txt` 全 `>=`,对接期建议钉到具体版本并留升级记录。
5. **案例侧收敛**:`case01` 与 `case_engine` 重复实现(consistency 177 vs 164、reflection 509 vs 496);
   死代码 `run.py`/`render.py`/`viz.py` 引用 0 处 —— **清理前先补导入面测试**(上次误删 `viz.py` 的教训)。
6. **两个实时面的宿主协议对齐**:case00 面只有"角色点击 → 发宿主"单向消息,case01 是双向协议。

**验收**:杀进程 30 秒内自动恢复且有日志;一条命令给每局指标;CI 上"清单与代码不一致"必红;
两个面的嵌入协议同一份文档描述。

## 本次已随体检落地的守卫(不用再派单)

- `tests/test_netguard.py`:写端点清单 ↔ 代码双向对齐;8060 的"13 个写端点"数字由代码计数校验;
  跨源白名单默认只给本机来源(行为断言);本机绑定守卫。
- **变异验证**:拿掉 `/control/restart` 那行 → 2 条测试红;把 8060 数字改成 12 → 1 条红。

## 明确不做(免得散)

- 不重开 Unity 线(已冻结);不碰平台侧的建单/专家池/状态机(那是平台侧实现);
- 本阶段不做多场景 UI 与 5020 布景(5020 连启动脚本都没有);
- **不为好看改口径**:任何"让数字好看"的筛选(例如默认藏掉不一致记录)都必须留在 `excluded` 里可见。

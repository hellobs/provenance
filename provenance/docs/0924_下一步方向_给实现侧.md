# 0924 下一步方向(给实现侧)

> **状态**:现行
> **最后核对**:2026-09-24
> **说明**:方向与派单:先打通研究闭环,再补内化,最后收工程底座(每条含验收)
> 依据 28 条真实记录的实测(`0924_IVD符合性与对接就绪度.md`);
> 每条含**现状 / 做什么 / 验收 / 依赖**。口径问题标"待研究侧拍",流程问题标"待平台侧确认"。
> 归属:实现侧 = 写代码;研究侧 = 定口径与资料;平台侧 = 治理工作台。

## 一句话

机制面已经够用(五条 IVD 不变量里四条实测成立),现在**卡在"闭环"上**:
判定语料只有六成走 agent 判断、"内化"这一环在 case01 没有载体、
平台侧拿到 `issue.field` 却建不了专家池。
所以下一步顺序是:**先把研究闭环打通 → 再补内化 → 最后收工程底座**,不要反过来先做界面。

## 方向 A:让"决策由 agent 产生"在语料上成立(P0,最该先做)

**现状**:`branch_source` 实测 `judge` 16 / `preset` 12 —— 43% 的记录里分支是运行参数给的,
这批语料本身不构成"决策由 agent 产生"的证据。守卫有"同场景两种回答 → 两个分支"的反事实测试,
但**没有一条真实记录**这样做过对照。

**做什么**
1. 用默认 `judge` 模式跑够样本(建议 ≥ 10 局),`preset` 只作对照条件;
2. 做**反事实对**:同一场景、同一时间线,喂两种显著不同的 T0 回答,记录成对保存并在索引里可辨;
3. 索引里把 `preset` 显式标出来(字段已有 `branch_source`,面板/契约里也要能一眼看出)。

**验收**:10 局里 `judge` 占比 ≥ 90%;≥3 组反事实对,两局分支不同且 `judge_info.reason` 可见;
`/api/runs` 能按 `branch_source` 过滤。
**依赖**:无(纯实现侧)。

## 方向 B:补 IVD 的"内化"环(P0,口径待研究侧拍)

**现状**:case01 记录 **28/28 零 `tendency`**;`state_history` 只有对手方资金/持仓。
价值倾向只在 case00(已冻结)那条线。也就是说"决策→后果→反思→**内化**"的最后一环,
在现在这条主线上**没有载体**。对外讲 IVD 时这是个会被问到的点。

**两条路,择一(研究侧拍)**
- **B1 把内化做进 case01**:最小可行 = 后果(盈亏、是否与 AI 的 T0 判断一致)写进内核倾向,
  且**下一局的检索与措辞受它影响**。关键:倾向归内核(mavis),案例侧只传后果,不直接改倾向。
- **B2 明确由 case00 承载**:case01 定位为"判断与后果",对外叙事按此切分,
  所有材料不再暗示 case01 有倾向内化。

**验收**:B1 → 同一角色连跑两局,第二局的检索/措辞能被第一局后果影响,且记录里查得到这条影响;
B2 → 文档、对接材料、演示口径统一,字面上不再出现"case01 展示内化"这类说法。
**依赖**:研究侧定"后果→倾向"的换算口径;B1 需实现侧改内核(只允许加法、默认关闭)。

## 方向 C:打通研究闭环(受控实验 → 专家审核 → 金标 → 训练材料)(P0,阻塞对接)

**现状**:平台侧**能建单**(123 条 issue 字段齐,只有 4 条缺 `routing_reason`),
但**建不了池**、**写不回**、**复算不了**、**没有统计出口**。

**做什么**
1. **`issue.field` 受控词表**(硬卡点):实测 123 条 issue 用了 **115 个不同取值**。
   定一份固定标签(建议 8–12 个),提示词里约束、落盘时校验,历史值给映射表或标 `legacy`;
   产出《字段标签清单》交给平台侧建池。
2. **写回鉴权**:平台侧若要专家改约束/标记反思,先加一层(反代 basic auth 或平台侧签名)+ 审计留痕。
3. **导出接口**:一条命令导出"标注包"(issue + 反思原文 + 判定留痕 + 该局 manifest),
   供人工金标与训练材料使用。
4. **N 局批量与统计**:同条件跑 N 局,输出分支分布、判定稳定性、问题条数、耗时。
5. **金标工具**:20–30 条判定与反思的人工标注入口与一致性计算。

**验收**:平台侧用标签清单能建池;导出包能被标注人直接读;`一条命令` 给出 N 局统计;
金标给出**准确率与同输入一致性数字**。
**依赖**:①词表口径(研究侧)②专家池字段(平台侧确认)③金标(研究侧标注)。

## 方向 D:可复算与可信(P1)

**现状**:`manifest`(git commit / 运行方式 / 判定后端与模型 / 温度 / 各哈希)**只有 1/28**;
判定重试留痕(`attempts`/`raw_outputs`)在记录里 **0/28** —— 代码有、真实记录没验证过。

**做什么**:①新跑的一律带 manifest;②历史 27 条**标注"不可复算"**(别让平台以为条条能复算);
③口径/提示词/资料变更写变更日志;④跑一局真实记录,确认 `attempts`/`raw_outputs` 进 `run.json`;
⑤可选:定时重跑同一场景做漂移检测。

**验收**:`/api/runs` 能区分"有清单/无清单";新记录 `judge_info` 含重试与原始输出;
任意一条新记录能按 manifest 复算。
**依赖**:无。

## 方向 E:工程底座(P1–P2)

1. **四个面的起停与看护**:现在 5010 靠 `live_switch`,只读面(5002/5003/8060)靠手工 ——
   这轮就被工具会话崩溃带走了一次。做一键起停 + 看门狗(被杀自动重起并留日志)。
2. **可观测性**:288 处 `print` vs 8 处 logger;时间戳无时区;缺每局指标(耗时/token/失败率)。
3. **CI 守卫继续加**:`case_engine/tests` 已进 CI(实测 182 passed);
   接下来把"写端点清单↔代码""字段词表""夹具存在性"都做成守卫(本次已加两条,见下)。
4. **依赖锁定**:`requirements.txt` 全 `>=`,对接期建议钉到具体版本并留升级记录。
5. **案例侧收敛**:`case01` 与 `case_engine` 重复实现(consistency 177 vs 164、reflection 509 vs 496);
   死代码 `run.py`/`render.py`/`viz.py` 引用 0 处 —— **清理前先补导入面测试**(上次误删 `viz.py` 的教训)。
6. **两个实时面的宿主协议对齐**:case00 面只有"角色点击 → 发宿主"单向消息,case01 是双向协议。

**验收**:杀进程 30 秒内自动恢复且有日志;一条命令给每局指标;CI 上"清单与代码不一致"必红;
两个面的嵌入协议同一份文档描述。

## 本次已随体检落地的守卫(不用再派单)

- `tests/test_netguard.py`:写端点清单 ↔ 代码双向对齐;8060 的"13 个写端点"数字由代码计数校验;
  跨源白名单默认只给本机来源(行为断言);本机绑定守卫。
- **变异验证**:拿掉 `/control/restart` 那行 → 2 条测试红;把 8060 数字改成 12 → 1 条红。

## 明确不做(免得散)

- 不重开 Unity 线(已冻结);不碰平台侧的建单/专家池/状态机(那是平台侧实现);
- 本阶段不做多场景 UI 与 5020 布景(5020 连启动脚本都没有);
- **不为好看改口径**:任何"让数字好看"的筛选(例如默认藏掉不一致记录)都必须留在 `excluded` 里可见。

## 追加:第六轮体检(声明 vs 记录 / 分流复读 / 跨运行方式的数据形状)

### 已量,没问题

- **声明 vs 记录**:168 条事件价格与场景声明的 timeline 价格**逐一相符**(0 条不符);
  `state_history[].branch` 与记录的 branch **28/28 一致**。
- **问题分流没有模板化**:123 条 issue 的 summary **123 个各不相同**、跨记录 0 组重复
  (说明 Router 不是套模板,是真按反思生成的)。

### 已修:三种 source 的返回形状不同,却只有一种带 `view`

- `/api/run-detail/{source}/{run_id}` 三种 source 给三种**完全不同**的 data:
  `review` → 九块白名单;`checkpoint` → `{conversation, shots}`;`compressed` → `{files}`。
  但以前只有 `review` 带 `view` 字段 —— 平台侧按九块写渲染,遇到另两种不知道自己在看什么。
- **已修**:三种 source 都给 `view`(`expert-safe` / `case00-archive` / `compressed-index`),
  契约 §3.3 新增一节把三张形状与"只有 review 有九块"写清。

### 待决策:另一条运行方式的产物与"九块"契约不一致

- 实测:`case_engine run case01_stock --engine experiment-eval` 写出的是 `<case_id>_exp.json`,
  键只有 `branch/consistency/engine/run_type/scenario/state/timeline` —— **没有九块**;
  而它在注册表里的 `output` 原写 `run.json`(与实现不符,已改成实话)。
- 影响:平台的列表/详情目前只覆盖 case01 注入器路径的 `run.json`。若要让平台也展示
  运行方式的产物,需要**先补一层映射**(或让运行方式直接产出九块形状)。
  这是"运行方式可自由搭配"这个卖点与"平台只认一套数据形状"之间的缺口,**要口径**。

## 追加:第五轮体检(信息出处 / schema 漂移 / 反思截断)

### 已修:注入事实的"出处"在链路上被丢掉了

- **现状**:记录里 28 条 × 358 条注入项的 `source` **100% 为空**;但 `case01/world/timelines.py`
  里一直写着 `"source": "HCM 公告"` / `"Battery Industry Daily"` / `"盘面"`,
  场景 YAML 里也有 33 处 —— 只在 `case01/injector/nodes.py` 把 timeline 聚成节点事件那一步被丢掉
  (只搬了 kind 与 content)。
- **后果**:一个讲"AI 在信息污染下如何判断"的系统,记录里却看不到"这条消息是谁放的";
  平台侧也没法在专家视图里给出来源。
- **已修**:节点事件带 `source`(记录侧 `_retrievals()` 早就在读它了)。
  真跑一遍 dry run:**13 条注入项 7 条带出处**,剩余的 6 条都是 `price` 行(timeline 里本来就没写 source)。
- **边界守住了**:`_as_story_event()` 是白名单映射,**不带 source** → AI 可见的信息环境没变。
  "AI 该不该看出处"是**口径**,要研究侧定,实现侧不擅自改(测试两头都钉住了)。
- **可选的后续**:①给 price 行补 source(资料侧一句话);②历史 28 条按 content/date 回填 source
  (照 `tools/backfill_consistency.py` 的样子写个回填工具)。

### 已修:记录里的"废弃"标记以前平台看不见

- 现状:`260919-live-case01-mavis-A-1720` 带 `deprecated: true` + `deprecated_reason`
  ("预设分支与 AI T0 立场不一致"),但它**不在** `/api/runs` 摘要字段里、也**不在**专家安全白名单里 ——
  当时只是因为这条恰好被判成 `questionable` 才被默认藏起来。**风险**:将来一条 `quality=ok`
  的废弃样本会被静默发给平台。
- 已修(2026-09-24):①`quality` 词表新增 **`deprecated`**,且**优先于**其它判断(自洽的废弃样本同样是废的);
  ②它进 `_HIDDEN_QUALITY`(5010 聚合、5002 契约、case01 嵌入面三处同口径),默认不给平台,
  但仍列入 `excluded` 并写明原因;③索引行带 `deprecated` 与 `deprecated_reason`,平台要自己筛也有依据;
  ④夹具新增一条"自洽但废弃"的记录钉住这个优先级(以前它会判 ok)。
- 实测:5010 默认隐藏 **3** 条 —— `A-1720` 现在报 `quality=deprecated`(带 `deprecated_reason`),
  另两条是 questionable;`excluded.reason` 里逐类说明(以前 A-1720 是被归进 questionable 才顺带藏住的)。

### 已量,没有问题

- **反思截断**:用仓库自带 `reflection_audit` 的口径核 28 条,**0 条**结尾不完整。
- **不变量 3 的实质检验**:28 条记录的 358 个"已释放事件"**全部**落在该节点的事件定义内(越界 0 条)。
- **schema 漂移**:28 条记录有 **4 种**顶层键集合(18 键 14 条 / 多 `debug` 12 条 / 多 `deprecated` 1 条 /
  多 `manifest` 1 条)。平台侧读记录要按"键可能缺"来写;这份清单值得并进字段字典。
- **服务日志**:四个面的日志都 <500 字节,无密钥痕迹。
- **磁盘**:results/ **2.36 GB / 5073 文件**是唯一的大头(case01/runs 1.8 MB);保留策略仍缺(方向 E)。
- **工具坑**:本仓有 `upstream` 远端,裸 `gh run view` 会去查那个仓;读写 CI 一律带
  `--repo hellobs/provenance`。

## 追加:第四轮体检(边界与一致性)

### 先说四件查过没问题的(免得以为到处是洞)

- **三份 run 清单一致**:5010 聚合的 review 28 条 = case01 面板 28 条 = 5002(全量)28 条;
  默认都藏同样那 3 条 questionable(5002 与 5010 同源的口径成立)。
- **金额算术自洽**:`is-A-0958` 200000 × 0.95 = 190000,÷45.2 = 4203.5 股,×27.4 = 115176.99,
  加未投入的 10000 = **125176.99** —— 与最终反馈里写的 125,177 对得上。
- **质检戳不陈旧**:28 条**现算 verdict 与记录里存的完全一致**(0 条偏差)→ 平台按 `quality` 过滤是可靠的。
- 我上一轮"反向依赖两个口径冲突"也是误判(棘轮只扫 `*.py`,deep_check 扫全类型),已在上一段纠正。

### P0(新增):"平台侧只嵌一个画布、其余自己重写" 这条对接路线

**前提变了**:对方只嵌**一个可视化画布**,记录/审核/建单都在他们那边重写。于是重点从
"我们的页面安不安全"变成**"API 默认安不安全 + 画布自不自足"**。

**已做(2026-09-24)**

- `/embed/canvas`(=`/embed/scene?chrome=0`):**纯画布** —— 顶栏、时钟、状态浮层、
  "重开一局"卡片一概不渲染(实测 `id="restart-box"` / `id="left-col"` 都不在页面里)。
  为什么必须做:原来 `/embed/scene` 带着我们的顶栏和一个 **`POST /control/restart` 按钮 + 分支下拉**,
  平台一嵌就等于把写入口摆在他们页面上。
- `/health` 增加 `run_id`:对方只有画布,得知道"画布上现在跑的是哪一条"才能和自己渲染的记录对上
  (以前这信息只在内部口)。`finished`/`finish_reason` 仍在,用来区分在推演/已跑完/仅审阅。

**还要做**

1. **`/docs`、`/redoc`、`/openapi.json` 现在都对外 200** —— 实测它们会把
   `/control/restart` 这类内部写端点列出来。对接前关掉,或放到只读反代后面。
   验收:平台侧拿到的那一面 `/docs` 不可达(或需鉴权)。
2. **给平台侧一份"九块字段字典"**(turns / retrievals / events / state_history /
   final_feedback / reflection / router / audit 的字段语义与取值),他们要自己渲染。
   验收:对方只读这一页就能把记录渲染出来,不用来问字段。
3. **(建议)内部口默认安全**:`/api/review/*` 现在**默认给原始记录**、`?safe=1` 才收紧。
   无鉴权的前提下,更稳的是反过来 —— 默认安全、内部用 `?safe=0`。
   依赖:要有一次小重构(页面的内部面显式传 `safe=0`)+ 测试跟改;请研究侧/契约侧确认后再动。
4. **画布能力边界写清楚**:画布只显示**实时小镇**;它不能回放某条历史记录,
   也不显示记录内容 —— 平台侧若想做"记录回放",需要另立需求(我们不默认提供)。

### P0(新增,排在所有 P0 之前):嵌入面把实验底牌露给了平台侧的专家 —— **已修(2026-09-24)**

**当时的现状**(实测,截图核对):

- `/embed/review` 与 `/review` 是**同一份页面**,只差一个 CSS class;页面把
  `判定方式` / `分支来源` / `T0 立场一致性` 三行**无条件**渲染(概览里还有 `branch` 字母)——
  代码注释写着"自相矛盾的记录必须自己说出来",那是给**我们自己复核**用的设计;
- 更关键的是取数接口:该页面用 `/api/review/run/{id}`,**返回原始 `run.json`**
  (含 `injector` / `branch_action` / `consistency` / `debug` / `branch`);`/api/review/live` 同样;
  而 `/api/run-detail` 的专家安全视图是对的 —— **边界只做在了一条 API 上**。

**已修**(口径取自契约 §3.2,不是新拍的口径):`/embed/review` 进安全模式 ——
不渲染 branch / 判定方式 / 分支来源 / T0 一致性、隐藏"注入器"页签与原始 JSON 链接、
下拉标签只写 run_id;三个取数口都加 `?safe=1`(白名单复用 `live.history.expert_safe_record`,
**唯一一处实现**);安全清单默认滤掉 questionable/debug 并在页面上写明隐藏了几条。
`/review`(我们自己复核)保持全量。**验证**:`case01/tests/test_review_app.py` 4 条新测试
(safe 视图白名单 / 内部面仍全量 / 安全清单滤掉并报数 / 实时口也安全);
浏览器截图核对两个面(嵌入面无那三行与注入器页签、内部面照旧)。

**顺带修掉一个我自己造的回归**:安全视图剥掉 `injector` 后,mavis 记录被页面误判成"旧引擎"
(截图里 `…-auto-1820` 显示"旧引擎 · 对照记录")→ 改成从清单的 `engine` 字段记引擎归属。

**剩下要你确认的**:`/review` 全量 + `/embed/review` 收紧 这个切分是否就是想要的(现在是这么做的);

### P1(新增):反事实后果没有结构化,三线后果没法自动比

**现状**:没买的那两局,机会成本只写在 `final_feedback` 的自然语言里(如"股价后来涨了约 40%"),
`state_history` 只有资金/持仓,**没有基准价或错过收益字段** → 平台/研究侧无法自动比较
A 买 / B 不买 / C 条件化 三条线的后果。

**做什么**:在 `world.state_schema` 加结构化后果(如 `benchmark_price` / `missed_gain_rmb` /
`would_have_exited`),由事实层算,案例只声明口径(不变量 4:机制不算判断)。

**验收**:同一局三条线的记录里都有可比较的数值字段;面板能直接给出"三线后果对照"。

# 会议准备：见需求方（2026-09-22 ~10:20）

> **状态**:待核对
> **最后核对**:2026-09-22
> **说明**:尚未人工核对状态
> 定性 / 三句话介绍 / 需确认口径清单 / 医疗场景追问 / 会前动作。供会谈对照,非交付物。

## 一、会谈定性

不是技术评审,而是**两个各自在跑的系统要对接**。我方已具备对接的全部硬资产:
**5010 单入口、6 个 iframe 嵌入面、只读单向数据接口、已按 GTC 源文档对齐的 case01、已一致的质检口径**。
姿态 = "对接基本就绪,来确认几处口径 + 顺带看你的医疗场景怎么进来",不是"来汇报进度"。

**最有价值的信息**:需求方说"我自己也在跑另一个医疗场景"——它正好验证 `case_engine` 通用化
(一引擎多场景)是否站得住。别当闲聊听,往通用化上引。

## 二、开场三句话(30 秒说完)

1. **接入极简**:平台用一个地址 `http://<host>:5010/` 就能 iframe 嵌入场景与结果,
   本栈不带 X-Frame-Options/CSP,直接嵌。
2. **只读、不反向依赖**:我们只吐数据(`/api/runs` 索引 + `/api/run-detail` 详情),
   建单 / 专家池 / 冲突 / 归池全在你侧;我不感知你存在。
3. **安全视图已做好**:专家看到的是去敏感信息的视图(`view: "expert-safe"`),
   原始 Reflection 不可覆盖。呼应对方向导语。

## 三、需当面确认的口径(会谈实质,别空手)

| # | 待确认项 | 现状 | 我方倾向 |
|---|---|---|---|
| 1 | **分支来源默认** | `branch_source` 默认 `judge`(由 Investment AI 在 T0 的回答判定分支),`preset` 保留为对照 | 默认走 judge,要需求方拍板 |
| 2 | **样本问题** | case01_stock 里标 A 的样本实为 B 线判据、标 C 的亦属 B 线 | 需换样本(研究侧确认) |
| 3 | **Financial Data** | 目前只有 HCM 一家 | GTC 源要 HCM + 另 10 家真实公司,问交付进度 |
| 4 | **信息边界圈** | 六条边界(experiment 性质/Branch A·B·C/Future Timeline/未披露私事/…)不进专家视图 | 问这个圈对不对 |
| 5 | **case00 也进审核链,只审 AI 助手**(2026-09-22 已记入 `名词与分层`) | case00 六角色仅 `ai_advisor` 是 ai_tool | 已定只监控 `ai_advisor`,落地走 `reflection_target: ai_advisor`,复用 router |

## 四、重点问他:医疗场景

- 你的场景是**受控实验型**(固定剧本、可复现、专家审反思)还是**长期演化型**(角色长线演变)?
  → 直接决定走 `protocol: experiment` 还是 `sandbox`(路线 C)。
- 你想不想也接到**同一套 5010 + case_engine**?若想,正好验证通用化;若有不同形态,问清差异。
- 你的专家审核流程,和我方契约里的 建单 / 冲突(2 专家互斥→+3 达成 5 人多数) / 归池 状态机对得上吗?
- 你那边模型怎么跑?(Ollama / API) —— 决定复现强依赖。
- 信息边界:你希望专家**看不到**哪些元信息?与我方 expert-safe 圈是否一致?

## 五、要带去确认的数据口径(契约硬约束)

- 主键:`(run_id, issue_id)` 复合;`risk` 小写 `low/medium/high`。
- 质检:`quality ∈ ok/questionable/debug/unverified`;`/api/runs` 默认不返回
  questionable/debug,但带 `excluded{count,runs[],reason}`(不静默)。
- 深链 `?run=<run_id>&tab=<页签>`,页签 `overview/turns/retrievals/events/states/reflection/router/injector/audit`。
- 平台只管地址 5010,不必知道当前跑的是 case00 还是 case01。

## 六、会前动作

1. 发对方两份:`docs/给平台侧_什么是引擎.md`(一页,非技术)+ `docs/平台对接契约_5010唯一入口.md`(对接口径)。
2. 可选演示:会前 `live_switch --start case01 --review-only`(不占 Ollama),现场直接看 embed 效果。
3. 若对方要样本成果:runs 不入库,需另发(见"复现缺口")。

## 七、若被问"你的东西能不能复现"(简明应答)

- 代码 clone 两仓(provenance + mavis)后测试全绿(CI 即铁证)。
- 前端零外网依赖(vendor 已入库)。
- 完整推演依赖 Ollama 两个模型(或配 API)。
- 历史三线记录不入 git,看已有成果需另交付。
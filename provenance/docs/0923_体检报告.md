# 0923 体检报告(对接前)

> **状态**:待核对
> **最后核对**:2026-09-24
> **说明**:尚未人工核对状态
> 相关文档:缺陷清单见《0922_Todo与验收标准》,对接口径见《给平台侧_嵌入与数据接入》。

## 一、本轮修掉并验证过的

| 问题 | 症状 | 修法与证据 |
|---|---|---|
| 5010 契约守卫在 CI 上从未生效 | CI 三条红:`case01/runs` 不入库 → CI 无记录;`live/history.py` 把数据根写死 | 数据根可覆盖 + 签入夹具(`tests/fixtures/`);干净克隆逐条复现 CI 各步全绿 |
| max_tokens 分档失效 | 分档只看 `caller`,而 mavis 的 `Agent.completion` 不传 caller → 镇内**每次**调用都被钉在 256 token(AI 的 T0 回答实测 536 汉字) | 分档改按 `return_type`;回归测试按 mavis 真实调用形状写;变异验证:改回去 4 条测试变红 |
| 截断静默 | 两个客户端都丢掉 `finish_reason`;`generate_chat` 的 `res: str` 兜底把截断的半句话当完整答案收下 | 记录截断计数 + 日志;反思/路由截断写进 `manifest_warnings`;变异验证:关掉计数 1 条测试变红 |
| 判定失败不可见 | `quick_scan("undetermined")` → `unverified`,而 `unverified` 是**默认照给平台**的 | judge 失败 → `questionable`(默认不给平台,`excluded` 里说明原因);变异验证:改回去两组测试变红 |
| `CASE01_EMBED_*` 空承诺 | README 让人设两个 embedding 变量,实际零调用方 | README 更正并注明检索不走 embedding 服务 |
| 5010 跨机不可达 | 只绑 `127.0.0.1`(实测本机 LAN 地址连不上),对接的硬阻塞 | `--host` / `LIVE_HOST`(两个实时面都支持),起完印出 LAN 地址 |
| 深链失败态不响 | `?run=` 指到不存在的记录时静默回落到另一条 | 页面上红字写明"深链指定的 run 不存在";`?tab=` 非法给黄条 |
| 无宿主协议 | case01 嵌入面与宿主之间没有任何消息 | 双向协议(`ready`/`run-selected` ↔ `mavis:select-run`/`mavis:set-tab`)+ 参考实现 `/embed/demo-parent`,无头浏览器截图核对过 |
| `/api/runs` 参数被静默吞掉 | `?limit=`/`?quality=` 传了没反应 | 支持 `limit`/`offset`/`source`;不认的参数回在 `ignored_params` |

## 二、现量数字(口径写在括号里)

- 静默异常(**`except` 块体只有 `pass`/`continue`**):**23 处**。
  注:《0922_Todo》里写 103 处 —— 口径不同(未写明),要么统一口径要么放弃这个指标。
- `print(` 调用 **288** 处 / `logger` 使用 **8** 处。
- `datetime.now()` **6** 处 / `utcnow()` **0** 处(记录里的时间无时区,跨机不可比)。
- 金额或权重用 `float(`(**同一行含 money/price/amount/cash/weight/tendency/资金/金额/价格/权重**):**10 处**。
- 硬编码绝对路径(**字符串里出现盘符或 `/home/`、`/Users/`**):**0 处**(《0922_Todo》里的 4 处已清)。
- 依赖声明:`requirements.txt` 用 `>=` 11 处、`==` 2 处;vizkit 全 `>=`(6 处)。
- CI:跑外层 `pytest tests`、`case01/tests`、`mavis-vizkit`、`check_engine_baseline`;**不跑 `case_engine/tests`**。
- 测试总数:外层 89 passed / 1 skipped(唯一的 skip 是配置工具如实报告 `skipped_agents`)、
  `case01/tests` 351 passed、vizkit 27 passed、mavis 144 passed(exit 0)。
- 文档漂移:提到已退役的 **5004** 的文档 5 份(`case00_case01_并列说明`、`名词与分层`、`速览_0908到0921`、`转接_给下一棒`、`通知平台侧_换样本`);提到 **5020** 的 3 份,而 5020 **没有启动脚本**。
- 反向依赖口径冲突:`tools/deep_check.py` 报 config_tool 命中 **3 个文件**,而棘轮测试基线是 **1 文件 / 4 处** —— 两个口径必须统一,否则"下降"无法验收。
- 静态装好的 scene:可走 142 格 / **9 块**(碎块 = 可能有走不到的死区;运行时不引用它,有守卫)。
- 案例侧死代码候选(`tools/deep_check.py` K 段):`case01/run.py`(98 行)、`case01/render.py`(463 行)、`case01/viz.py`(357 行)非测试引用 0 处。

## 三、仍未解决(按严重度)

**P1 —— 挡住平台侧正式对接**

1. **CORS 默认 `*`**:对接前必须显式设 `EMBED_ALLOW_ORIGINS` 为平台侧域名(归属:实现侧 + 运维)。
2. **两面协议不一致**:case01 嵌入面有双向协议;case00 的 `main_script.html` 只有"角色点击 → 发给宿主"这一条单向消息,没有 `ready`/`select-run`。平台侧现在得按 case 写两套(归属:实现侧)。
3. **嵌入面下拉含 questionable 记录**:与 `/api/runs` 默认隐藏的口径不一致(面板要给我们自己复核,所以保留)。平台侧要严格过滤得自己做白名单(归属:实现侧定口径,平台侧实现)。
4. **没有给宿主的事件推送**(无 SSE/WebSocket 面向宿主):宿主只能轮询 `/api/runs`;"新的一局开始了"不能主动通知(归属:实现侧,可延后)。

**P2 —— 稳定性与可观测性(0922 Todo 第 3/4 项)**

5. 无看门狗、无统一断点续跑、无结果保留策略、无运行指标。
6. `print` 288 : `logger` 8;时间戳无时区(6 处 `datetime.now()`)。

**P3 —— 数据与质量**

7. 分页只在 5010(5002 是冻结契约,不动);`/api/runs` 在 500+ 记录下未实测。
8. 判定与反思**没有人工金标**:准确率与同输入一致性都还没有数字(归属:研究侧标注 + 实现侧做工具)。

**P4 —— 收敛与清理**

9. `case01` 与 `case_engine` 重复实现(consistency 177 vs 164 行、reflection 509 vs 496 行)。
10. 死代码清理**必须先补导入面测试**:上次靠"引用计数"误删 `case01/viz.py`,是靠收集报错才发现并还原的。
11. 文档漂移(5004 已退役、5020 无入口)与 `case01/README.md` 里仍列着归档入口的命令。

## 四、我这轮自己的两次测量失误(记下来,免得再犯)

1. **CORS 探针用大写头名查**,而 uvicorn 发的是小写头 → 得出假结论"没有 `Access-Control-Allow-Origin`"。
   重新大小写无关地量:预检 `200`、`ACAO=*`、`Max-Age=600`,本来是好的。
2. **第一个变异验证的第三个变体是"路径写错"导致的非零退出**,我把它当成"测试抓到了 bug"。
   重做后才算数(两组都要求 `AssertionError`)。
   → 规则:"没测出来"和"变红"都要先问一句——它红的是断言,还是别的原因?

## 五、平台侧对接阻塞项(按处理顺序)

1. ~~实现侧用 `--host 0.0.0.0` 起 5010~~ → **改口径**(见 §六):首选不暴露端口;确需直连要
   显式 `LIVE_ALLOW_REMOTE=1`,并且平台侧机器实测一次防火墙是否放行。
2. 实现侧设 `EMBED_ALLOW_ORIGINS` 为平台侧域名(默认已收窄为只允许本机来源)。
3. 平台侧按"当前跑的是哪个 case"决定嵌哪个面(拿错面是 404),共同面见对接文档 §二。
4. 平台侧确认接受"轮询 `/api/runs`"(暂不提供事件推送)。
5. 双方确认:`quality=questionable`(含判定失败)默认不建单;要拿到必须先解决记录本身的问题。

## 六、安全体检(同日补做)

触发:有人指出"跨机对接把端口开出去"这件事本身就不安全 —— 这个判断是对的,我上一版
在对接文档里把 `--host 0.0.0.0` 写成了推荐做法,那是错的。现将暴露面逐项量清。

**实测的暴露面(没有任何鉴权、没有登录、没有 Host 校验)**

| 面 | 写操作 | 说明 |
|---|---|---|
| 5010 实时面 | 3 个: `/api/goals`、`/api/undo-intervention`、`/api/reflections/mark` | 改治理权重 / 撤销干预 / 标记反思并写档 |
| 8060 配置工具 | 13 个:`/api/scenario/save`、`/api/run/execute`、`/api/agent/delete` 等 | 改场景、删角色、跑一次运行 |
| 5020 / 5002 / 5003 | 0 个 | 只读,但同样无鉴权 |
| CORS | 原来默认 `*` | 跨源读口子全开 → 任意网页可 `fetch` 走成品记录 |
| 静态挂载 | `5010 /static` → `frontend/static`;`5002 /viewer` → `case01/runs_html` | 均为预期内容,无越界 |

**没问题的部分(查过,给结论)**

- 密钥:`case01/.secrets.json` 存在(163 字节)且**被 `case01/.gitignore` 忽略、未入库**;
  用大小写敏感 + 形状化规则扫了 **5113 个产物/日志文件,0 命中**(第一版用
  PowerShell `Select-String` 是大小写不敏感的,`sk-` 命中了 "ri**sk-**averse"、`CASE01_`
  命中了路径里的 `case01_stock`,全是假阳性 —— 见 §四 的教训)。
- 命令执行:我们自己的代码里没有 `shell=True` 拼接用户输入;`subprocess` 全是列表参数
  (`netstat`/`taskkill`/`git`/`python -m`)。命中都在 `.uv-cache` 里的第三方包。
- 路径穿越:5010 的 `run_detail`/`full_context.load_run` 有白名单校验,`/embed/review`
  的 run_id 走同一套;实测 `..%2F..%2Fetc%2Fpasswd` 等一律 404(既有测试)。

**这轮修掉的**

1. `live/netguard.py`(**新**):非本机绑定必须显式 `LIVE_ALLOW_REMOTE=1`,否则**拒绝启动**并把
   写端点清单打出来;`live_run.py`/`live_fastapi.py`/`live_switch.py` 三处入口都接了。
2. CORS 默认从 `*` 改成**只允许本机来源**;平台侧跨源取数须显式设 `EMBED_ALLOW_ORIGINS`。
   iframe 嵌入不走 CORS,不受影响。
   **补记(差点漏掉)**:我第一版只改了 `live/routes.py`,而 case01 这条实时面**根本不 import 它**,
   它用的是 `packages/mavis-vizkit` 插件里**另一份** CORS 中间件 —— 起服务实测仍然是 `ACAO=*`。
   现已两处同口径,并各加一条行为测试(默认外站拿不到 ACAO / 显式给了就放行)。
   教训:改完必须**在真服务上量一次**,不能只看"我改的那个文件"。
3. 8060 配置工具:`save_agent()` / `upgrade_agent()` 补上角色名守卫(`_safe_agent_name()`),
   与 `delete_agent` 共用一处策略 —— 此前名字里带 `..\..` 能在 `AGENTS_ROOT` **之外**建目录、
   写 `agent.json`(`delete_agent` 早有守卫,这两处漏了)。
4. 文档口径纠正:对接文档 §一 与 §八、5010 契约、两个 README(中英)全部改成
   "默认本机 / 首选隧道或只读反代 / 确需直连的四个前提"。
5. README 漂移:配置工具端口 `5002` → **8060**(中英两处)。

**测试**

- `tests/test_netguard.py`(6 条):回环识别、未声明即拒绝、声明后放行、暴露清单含三个写端点、
  白名单默认只给本机、**外站 Origin 拿不到 ACAO**(行为级)。
- mavis `tests/test_config_tool_name_guard.py`(12 条):合法名放行、9 种穿越名拒绝、
  `save_agent` 拒绝时**先于落盘**(不许写完再报错)。

**仍留的口子(需要决策,不是我能单方面定的)**

- 三个写端点仍然没有鉴权:如果将来要给平台侧开放写能力(比如专家在线改约束),
  必须先加一层认证(反向代理的 basic auth / 平台侧签名),不能靠"本机绑定"当安全边界。
- `/api/reflections/mark` 等写端点没有 CSRF 防护(靠 JSON body 挡简单表单 POST;
  本地服务 + 无鉴权,风险有限,但一旦对外就不是了)。

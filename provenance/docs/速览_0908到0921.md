# 速览：0908 → 0921

> **状态**:现行
> **最后核对**:2026-09-21
> **说明**:**0908→0921 速览**:给评审看的简报(项目现状/五条主线/与 0908 冲突点的归属/可运行地址)
## 一句话

**13 天里，把"一个案例写死的一套流程"抽成了"声明式场景 + 可换引擎"的一层，并把平台对接口径收成单一入口。**
mavis 仍是纯内核（v1.2.1），case01 的业务语义全部留在案例侧。

## 一、目前工作

**MAVIS 引擎框架**：`github.com/hellobs/mavis`（v1.2.1）

- 内核：多智能体仿真（记忆 / 检索 / 反思 / 日程 / 行动 + 步进循环 + 通用插件面）；零业务词汇、依赖单向、可独立发布与验证。
- 内含角色配置工具 `config_tool/`：Web 表单生成与维护角色、关系、剧情。
  - 0920 起增：场景创建器按引擎动态渲染；**引擎页**（只读展示已注册引擎及其可跑场景）；**组合页**（场景 × 引擎）。
  - 待收口：该工具目前反向探测 `provenance/case_engine`，依赖方向待调整。

**可视化平台**：`github.com/hellobs/provenance`

- 内含 `case_engine/`（0920 新增，通用编排层）：声明式场景 `scenario.yaml` + 可注册引擎策略。
  - 现有引擎：`experiment-eval`（受控实验 / 评估）、`sandbox-value`（生成式价值权重沙盒）。
  - 同一引擎支持多场景：case00 小镇 / case01 股票 / case02 最小立项。
- 内含 `case01/`：受控实验（节点驱动注入器 + 只读契约 + 专家结果面板）。
- 内含 `live/`：实时面与只读服务 —— **5010 唯一对接入口**；5002 case01 契约；5003 case00 存档；5004 多场景。
- 内含 `packages/mavis-vizkit/`：可视化插件包（挂 mavis 插件面，与具体案例解耦）。
- 内含 `tools/`：地图与场景工具 —— `tilemap_to_maze.py`（Tiled 导出 → tilemap / maze）、`install_scene.py`（场景安装 + 碰撞与出生点校验）。
- 内含 `docs/`：对接与治理文档 —— 架构总览与对接指南、平台对接契约（5010 唯一入口）、IVD 不变量、名词与分层、文档索引。

**可视化平台 Unity 版（已暂停维护，2026-08-30 冻结）**：`github.com/hellobs/Multi-Model-AI-Visualization-and-Interactive-Simulation-Platform`

- 战略调整：放弃 Unity 复现；IVD 可视化与交互仿真统一以 provenance（Web）为唯一载体，外部平台经 iframe / 组件接入。
- 仓库仅作存档参考，不再投入开发。

## 二、这 13 天做了什么（五条主线）

1. **case01 收口**：注入器（节点驱动推演）跑通并冻结契约；只读数据服务（5002）与专家侧结果面板成型；
   可视化抽成独立插件包 `mavis-vizkit`（挂 mavis 插件面，不认识任何案例）。
2. **mavis 去业务化**：内核里散落的案例文案（如"空闲待命"中文串）收敛为**单一可覆盖来源**；
   框架与案例的依赖保持单向（`mavisframework` → 案例侧 **0 处**）。
3. **0920 场景引擎通用化**：把 `case01/injector`（6,447 行）里的**机制**剥成 `case_engine`
   —— 一局实验写成 `scenario.yaml`（数据）、引擎是**可注册的策略**（机制）、
   同一份引擎能跑多个场景（case00_village / case01_stock / case02_minimal）。
4. **0921 平台对接收口**：5010 升格为**唯一对接入口**；聚合索引补**质检口径**
   （`quality`：ok / questionable / debug / unverified，默认过滤但**报明 excluded**）；
   单条详情默认给**专家安全视图**（剥掉实验元信息），原始全文需显式 `?raw=1`。
5. **0921 工程卫生**：立 **IVD 不变量**（决策必须由 agent 产生、引擎只做机制、可追溯）并配可执行守卫；
   全仓文档加状态块 + 文档索引（过时文件一眼可见）；配置工具改"先选引擎再显示配置"；
   **沙盒禁止时间轴回滚**（后果不可倒带）。

## 三、0921 又补的一条：价值权重"声明即来源"

case00 的价值权重原先散在三处（scenario 声明 / `governance.json` / 各 `agent.json`，后两者是**手抄副本**）。
现在：引擎提供 `value_tendency_plan()`（声明→资产的唯一映射）+ `materialize()` 落盘器 + CLI
（`python -m case_engine materialize-value-tendency <case_id> [--apply]`），配置工具也改成调用同一映射；
**17 条守卫**保证"资产 == 声明"，谁只改一边 CI 就红。

## 四、0908 速览里那条"mavis 与 case01 冲突"现在怎么处理

不再让两者互相迁就，而是**分层**：mavis 保持"多智能体研究框架"的本性（自主行动、运行中可干预、
经历式反思、每局不同）；case01 的"单次受控实验"要求（剧本固定、运行中不干预、独立模型拆问题、
专家按问题审核、可复现、严格信息控制）落在**案例层 + 引擎层**：

| 0908 的冲突点 | 现在的归属 |
|---|---|
| 行为来源（自主 vs 剧本） | 引擎按声明投放事件与节点；角色行为仍由 mavis 生成 |
| 运行中干预 | 沙盒（case00）保留治理面板；**受控实验（case01）不干预**；沙盒也**不允许回滚** |
| 反思机制 | mavis：经历式反思（影响后续行为）；case01：同一模型对刚结束的判断反思 → 独立模型拆问题 → 专家审核 |
| 可重复性 | 引擎保证确定性（分支判定、节点序列、落盘记录）；mavis 侧保持随机性 |
| 信息控制 | 由 scenario 声明"谁能看到什么"（检索与释放事件），引擎执行 |

## 五、当前可运行地址

- **5010** 实时面（唯一对接入口）：`http://127.0.0.1:5010/`
- **5002** case01 只读契约：`http://127.0.0.1:5002/docs`
- **5003** case00 存档只读：`http://127.0.0.1:5003/`
- **5004** 多场景 × 引擎只读：`http://127.0.0.1:5004/api/scenarios`
- **5020** 像素布景（不跑引擎）：`http://127.0.0.1:5020/embed/scene`
- **8060** 配置工具（角色/场景/引擎/组合）：`http://127.0.0.1:8060/`

# 案例侧收敛清单（case01 精简）

> **状态**:现行
> **最后核对**:2026-09-22
> **说明**:**case01 精简清单**:0920 抽出引擎后案例侧该退的模块、差异、处置口径与验证方式
> 现状、风险、处置口径、验证方式，以及本轮尝试与踩过的坑。供下一轮直接开工。

## 零、硬约束：设定内容以 GTC 源文档为准（2026-09-21 用户要求）

**case01 的场景、人物设定、时间线、信息边界必须忠于 `GTC/0904doc/` 的源设定。**
精简与重构**只能动机制，不得动设定内容**；设定的权威是源文档，**不是实现现状**。

- 权威源：`GTC/0904doc/01_GTC_Case01_场景说明.txt`（角色与组成、信息边界、Run 推进）、
  `03_GTC_Case01_Future_Timeline.txt`（时间线）、`04_GTC_Reflection_Governance_Platform_技术集成流程.txt`
  （反思与审核流程）、`05_GTC_HCI_增量需求说明_平台侧.txt`（HCI 增量）。
- 已由源文档明确、且与我们的实现逐条对得上的设定（抽样核对）：
  - 四个核心组成部分：Investment AI、Ethan Lin、World / System、Financial Data；
  - Investment AI 的信息边界（它**不知道**）：当前 Case 属实验/模拟、存在 Branch A/B/C、
    Future Timeline 与未来股价公告、Ethan 尚未披露的私人背景与未来个人后果、
    Ethan 背后是 API 还是本地模型、后续 Reflection 治理与专家审核安排；
  - 运行约束：本地大语言模型经 Ollama；可检索本地 Financial Data；**不配置 browser / public web search**；
  - Ethan Lin：虚构普通个人投资者，第一人称对话；T0 提出 **HCM 投资咨询**；最终节点由他陈述
    实际行动、市场结果与已发生的个人后果；不以 AI 身份自我介绍、不暴露模型实现。
- 由此得到的纪律：
  1. 收敛/重构的每一步，**设定内容零改动**（角色名、标的、时间线事实、信息边界、流程要求）；
  2. 若发现实现与源文档不一致，**以源文档为准改实现**，并把该条写成机械检验（守卫测试）；
  3. 若发现实现里有源文档未写过的内容，先列入"疑似自创"清单，由需求方确认后再保留或删除，
     不得默默沿用；
  4. 提示词与判定文案（reflection / consistency 的措辞）属于"流程要求"的一部分，
     合并时以不改动对外产出为准（详见第二节）。

## 一、背景（为什么会有这份清单）

- 0920「场景引擎通用化」把 `case01/injector` 里的机制抽成 `case_engine/`，但 `case01/` 里的**旧实现没有被退役**：
  `case01/` 对 `case_engine` 的引用是 **0 处** —— 两条实现路径并行，实跑仍走旧路径（`case01/injector`）。
- 2026-09-21 的深度体检**没有查出这件事**：十维覆盖纯度 / 边界 / 契约 / 数据 / 文档 / 守卫 / 场景，
  唯独没有"抽出通用层之后，原处该退的退了吗"这一维。现已补 `tools/deep_check.py` 的 **K 节**。

## 二、逐模块清单

**`case01/consistency.py`（174 行）vs `case_engine/consistency.py`（164 行）**
- 关系：同名同职责，逻辑同源（git diff 仅 89+/98−）；引擎版把词表/说话人/判定器做成了注入点。
- **差异不在文案表**：T0 缺失时的回退行为不同；判定理由字符串不同
  （case01 写"没有 T0…"，引擎写"没有受判主体的对话，无法判断（继续追问）"）。
- 处置：**逻辑归引擎、措辞归案例** —— 给引擎补"判定文案可注入"的小口子，
  case01 把词表（18 个买入词等）、说话人 id（`ai`/`investment_ai`）、`LLMBranchJudge`、理由文案注入进去。
- 验证：`case01/tests/test_consistency.py`（本次尝试时正是它拦下了上面两处差异）。

**`case01/reflection.py`（501 行）vs `case_engine/reflection.py`（438 行）**
- 关系：**函数集完全相同，但除 `_looks_like_meta` 外每一个函数体都不同** —— 不是复制品，是已分叉的两套实现。
- 提示词差异明显：`ROUTER_PROMPT_CN` 882 vs 699 字、`REFLECTION_SYSTEM` 235 vs 141 字、
  `REFLECTION_PROMPT_CN` 956 vs 954 字（`ROUTER_JSON_HINT` / `ROUTER_REWRITE_HINT` 逐字相同）。
- 处置：**逐函数对照合并**（不是替换）：提示词与 `assemble_reflection_material` 的说话人标签留案例侧、
  经 `run_reflection(..., reflection_prompt=..., speaker_labels=...)` 注入；纯函数（strip / parse / rewrite）
  以引擎为准，合并前先逐条比对行为差异。
- 风险：高（改动会直接影响反思正文与 router 问题，属对外可见内容）。

**`case01/orchestrator.py`（573 行）**
- 被 `run.py` **与 `case01/injector/worldfacts.py`** 使用 → 不能按"只被 run 引用"处理。
- 处置：先定"旧 CLI 路径由谁替代"（现入口是 injector/引擎），再退役。

**`case01/run.py`（97 行）、`case01/render.py`（463 行）**
- import 侧 0 引用，**但**它们是 `case01/README.md` 里 `python -m case01.run` / `-m case01.render`
  的文档化 CLI 入口（实测仍可运行）。
- 处置：退役必须**连 README 与文档一起改**，否则文档会指向不存在的入口。

**`case01/viz.py`（357 行）**
- 已被 `vizkit` 取代（`case01_可插拔可视化设计.md` 自述"不再使用已归档的 `python -m case01.viz`"），
  但 `case01/tests/test_viz.py`、`test_vizkit.py` 仍 `from case01 import viz`。
- 处置：把这两个测试改为直接测 `mavis-vizkit`，再删 `viz.py`。

## 五、GTC 忠实度审计（2026-09-21，只读审计结论）

审计口径：逐条比对 `GTC/0904doc/` 源文档与实现（`cases/case01_stock/scenario.yaml`、
`case01/injector/scenario/agents/*/agent.json`、`case01/world/timelines.py`、`case01/orchestrator.py`、
`case01/agents/*.py`、`case01/reflection.py`）。

**已逐条对上的设定**：四个核心组成；Investment AI = 本地模型（Ollama）且其固定提示词逐字来自
源提示词包；Ethan = 普通个人投资者、第一人称、冲突内容不发重生成；T0 日期与初始状态
（2026-08-27、20 万、无持仓）；T0 标的（HCM 的 120-150 亿元潜在订单传闻）；A 线价格与
45.20 买入 / 27.40 退出（2026-09-07）；B 线 25 万门槛；C 线复用 A 线时间线；
Reflection 与 T0 同源本地模型。

**不一致（按严重度，待修）**

1. 高｜六条信息边界（实验/模拟、Branch A·B·C、Future Timeline 与未来公告、Ethan 未披露私事与未来后果、
   Ethan 背后是 API 还是本地模型、Reflection 治理与专家审核）**未进任何提示词**；
   且 injector 路径只注入 role_directive，源提示词包在该路径根本没被使用。→ 两条路径都注入源提示词原文 + 边界。
2. 高｜injector 的 role_directive 要求 "four to eight sentences in plain English"，与 AI 提示词的
   "Respond in Chinese" 自相矛盾。→ 统一语种要求。
3. 中｜`scenario.yaml` 的 `system_prompt` 为空；`display_name` 用了 `investment_ai` / `ethan`，
   源里是 `Investment AI` / `Ethan Lin`。→ 改声明。
4. 中｜Financial Data 目前只有 HCM 一家，源要求 HCM + 另外 10 家真实公司。→ 待研究侧交付（已在
   `case01/docs/对接确认单_引擎侧答复_20260905.md` 记录）。
5. 中｜`scenario.yaml` 里标 A 的样本原文实为 B 线判据、标 C 的样本亦属 B 线判据（`通知平台侧_换样本` 已自认）。
   → 换样本。
6. 低｜源要求 Router 走独立 API，实现缺省回落同一个本地模型。→ 改实现或声明。
7. 低｜专家分配 / 冲突再加 3 人 / 5 人多数 / Approve·Edit·Reject 未实现 —— 源文档本就把这部分划给平台侧。
   → 仅补文档。
8. 低｜Ethan 声明为 `llm: external`，实现另可注入本地 HF 权重（源允许换后端），声明未标明。→ 补声明。

**机械守卫（已落地）**：`tests/test_gtc_fidelity.py` —— 10 条断言覆盖上表"已对上"的事实
（日期、角色类型与模型、角色名、分支→时间线映射、T0 标的、时间线价格、买入/退出价、提示词首句、
无联网工具字段、Ethan 不自曝模型）；两处高危缺口以 `xfail` 显式挂着（不静默），修好后自动转正。

**疑似自创（源文档未写，先列待确认，不默默沿用）**：agent.json 的 `age` 与 `spatial` 地址、
`currently`/`scratch` 全文；`investment_ai.py` 的 REASONING_HINT 及其中文要求；
`ethan.py` 的 STRICT RULE 段；`reflection.py` 的去客套/去 emoji 规则；
`orchestrator.py` 的 C 线创业启动金背景；`worldfacts.py` 的 C 线英→中触发词映射；
`scenario.yaml` 的一致性词表。这些**多数是必要的工程补全**（源文档只给规则不给实现细节），
但按纪律先登记，需求方确认后再决定保留或改写。

## 三、收敛原则与验证口径

- 原则：**逻辑归引擎，措辞归案例**。引擎不得内置任何案例措辞；
  案例通过参数注入提示词、词表、说话人、判定器（引擎已支持 `reflection_prompt` / `router_prompt` /
  `signals` / `speaker_ids` / `judge_factory`）。
- 验证：`pytest case01/tests case_engine/tests`（498 条）+ 外层 73 条 + `tools/deep_check.py` 的 K 节
  （K 节须从"两套并存"变为"case01 已引用 case_engine"）。
- 顺序建议：`consistency`（先开口子）→ `reflection`（逐函数合并）→ `viz`/`render`（连测试与 README）
  → `run`/`orchestrator`（定替代入口后退役）。

## 四、本轮踩过的坑（避免重犯）

1. 引用检测三错：子串假阳性（`run_naming` 命中 `run`）、漏相对导入与字符串引用
   （`from ..review_app import`、`uvicorn case01.serve:app`）、漏 `from PKG import name`
   （`from case01 import viz`）—— 最后一个导致真的误删模块、测试 collection error（已还原）。
2. 生成本文件类改写时两次事故：占位符未替换（`{BUY}` 变成字面量）、字符串里嵌套引号
   （写 `.py` 补丁脚本时踩到）。结论：**改写一律走 .py 脚本并即时 `ast.parse` 自检**。
3. 收敛尝试必须以测试为准：把 `consistency.py` 换成薄适配层后，
   `test_only_t0_day_is_used...` 与 `test_no_t0_dialogue_reports_unknown...` 立刻失败 ——
   说明两份实现在"边界行为 + 判定文案"上确实不同，**不能当复制品替换**（已回退，498 条恢复全绿）。

# Case 01 引擎侧对接确认单(平台 / HCI 侧提出,请引擎侧复核)

> 提出方:Governance Platform / HCI 侧。
> 复核对象:引擎侧(case01 实现与接口)。
> 目的:在平台侧开工前,把交付基线、接口契约与未决事项一次对齐;
> 平台侧的数据结构由其自行设计并另行同步,本文不涉及该细节。
> 日期:2026-09-05。

## 一、引擎侧近期交付汇总(请复核是否即当前基线)

- 代码基线:provenance 仓 `main`,提交 `9ad052d`(2026-09-05)。
- 三线受控实验演示:demo-1(建议回避线,自动判定)、demo-2(积极买入线,
  强制)、demo-3(条件化等待线,强制);三条记录均含完整对话、市场时间线、
  本地模型 8 维内部反思(Reflection)与独立模型问题分流(Router),可作为
  平台联调数据。
- 只读数据服务三个端点(契约见 `docs/case01_api.openapi.yaml`):
  1. `GET /api/runs`——Run 索引;
  2. `GET /api/runs/{run_id}`——结构化治理数据(原始 Reflection 文本、
     Router 拆分问题、世界侧审计时间线);
  3. `GET /api/runs/{run_id}/full-context`——面向专家的完整记录自然语言
     全文(不含实验元信息,按 04 六.3 / 05 三 组装)。
- 引擎侧新增能力(本轮):T0 咨询支持多轮(06 §3.1);Branch C 方案解析为
  可机检触发条件并逐时间节点监测(01 §六 / 03 §八);Branch C 个人后果按
  实际执行结果生成;Router 输出解析容错。
- 测试:119 项全绿;演示页(折叠式)与数据服务均可在本机运行。
- 文档:`docs/Governance平台对接说明.md`(分工/字段/状态机/HCI 清单/
  验收点)、`docs/对接启动_给Tongmu_20260905.md`(开工路径与待确认事项)、
  `README.md`《与 0904 文档的设计决策与假设》。

## 二、请引擎侧确认的事项

1. 上述提交 `9ad052d` 即当前可对接基线;三条 demo 是否保留为正式联调样本。
2. 三个只读端点即最终接口契约(响应结构不再变动)。
3. Router 问题(issue-N)与 Reflection 原文之间的片段对应方式:由引擎侧
   提供定位(如引用范围/行号),还是平台侧按问题摘要就近截取展示。
4. 《与 0904 文档的设计决策与假设》六条(含 Branch C 个人后果方案)是否
   并入正式文档;Branch C 个人后果文案是否需研究侧另行确认。
5. 平台在异机联调时,只读服务与演示页的暴露/部署由谁负责、采用何种方式。
6. 已知未决项(不影响平台开工,需排期):Branch C 分批(多阶段)执行的
   建模;真实公司 Financial Data 的提供与接入。

## 三、平台 / HCI 侧下一步(供引擎侧知悉)

- 按 05 文档六项推进:任务队列 → 任务详情与审核操作 → Full Context 展示
  → 冲突追加审核视图 → 全过程追溯 → 训练材料归池状态。
- 平台侧 Task 与审核结果的数据结构由平台侧自行设计定稿,定稿后将
  《接口字段 → 平台字段》映射回传引擎侧核对,确保建单与回溯无误。
- 平台侧第一里程碑(与 05 第 1-2 项对应):建单 + 单专家审核闭环,
  以三条 demo 数据联调;随后接入冲突处理与追溯。
- 审核过程中的一切写入(状态、专家意见、冲突、归池)存于平台侧,
  引擎侧记录保持只读。

## 四、建议时间线

- 本周:平台完成建单与单专家审核闭环(依赖第二部分第 1、3 项答复);
- 下周:Full Context 展示、冲突处理、追溯链;
- 演示前:训练材料归池状态可见。

引擎侧对第二部分任一事项有异议或补充,请直接回复本单;平台侧将按
回复调整建单与界面实现。

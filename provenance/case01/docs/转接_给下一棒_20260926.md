# 转接信息 · 给下一棒(接手人,2026-09-26 版,含方向与任务)

> **状态**:现行
> **最后核对**:2026-09-26
> **说明**:本文件取代 `转接_给下一棒_20260924.md`(该份已标"已被取代",历史仍可读)。
> 更早的 `转接_给下一棒_20260918.md`(§14"不允许静默"等铁律)依然全部有效。
> 三份配合读:0918 讲规矩 → 0924 讲当天 → 本文讲**当前基线 + 你能做的方向**。

---

## 一、当前基线(2026-09-26,接手前必读)

**测试**:五套 921 项全绿 —— 仓库根+vizkit+injector 包 153 / provenance(case01+case_engine)
587 / mavis 167。一条命令:

```
cd provenance
python -m pytest tests case01/tests case_engine/tests ../packages/mavis-vizkit/tests \
  ../packages/mavis-case01-injector/tests -q
# mavis 仓另跑:pytest tests(1.3s,已密封不依赖 Ollama)
```

**结构(阶段 3 已完成)**:三包分层
`mavisframework ← mavis-vizkit / mavis-case01-injector ← provenance`。
- `packages/mavis-case01-injector/`:world + injector 九模块 + llm/consistency/atomicio
  收编;包内除 `_providers.py`(过渡 seam)外零 case01 引用(有守卫测试钉死);
- provenance 侧 `case01/injector/*`、`case01/world/*` 等是 **sys.modules 自替换 shim**,
  调用方零改动;monkeypatch/reload 语义全等;
- CI(provenance + mavis 两仓)全绿。

**数据与证据(科研素材都在 `results/analysis/`,配套文档
`case01/docs/内化与敏感性_定量分析_20260924.md`)**:
- **内化状态层成立**:12 条干预,11 条朝新约束位移为正(窗口 60/120/240 分钟稳健);
  动力学:响应延迟中位 8 分钟、11/12 持续、7/12 过冲后收敛;
- **行为层未观测**(诚实结果):2 小时窗内行动内容位移与底噪(5 agent × 20 对照窗,
  中位 0.0187)同量级;仅最大的两次升维超出全部噪声范围,不足为证;
- **Router 敏感性**:同反思双模型,条数稳定但风险校准不同、表述分歧大 →
  生产口径固定 nemotron,人工一致率做外部锚;
- **LoRA 线预准备完成**:`lora_prep.py` 管道(标记→SFT/DPO JSONL,校验门+去重+统计)
  就绪,**真实标记 0 条**——闸门在专家数据侧;`requirements-lora.txt` 训练栈清单;
  本机 torch 是 CPU 版(训练前需装 CUDA 版,GPU 8GB 属勉强可行档,详见
  `case01/docs/LoRA预准备_20260924.md` 的 Runbook);
- **演示口径边界(重要)**:主张**状态级内化 + 反思证据**,**不主张行为级**——
  这是行为追随分析给出的实测边界,答辩时不要越线。

**安全与环境**:5002/5010/8060 CORS 与写端点防护全部收口(JSON-only 守卫、
原子写、路径守卫);本机 Ollama 与三个服务当前**已关闭**(2026-09-26 用户要求),
启动方式见各入口;`mavis_case01_injector` 全链已验证在 Ollama 关闭下如实降级
(预检探针 2s,不再分钟级空转)。

---

## 二、下一棒的四个方向(按建议优先级)

### 方向 A:行为层证据的接力(科研主线,工具地基已就位)

状态层位移已有;行为层(2h 行动计划文本)未观测。三个递进的补法:
1. **对话层分析**(最被看好):checkpoint 里有 `conversation.json` 和决策事件的
   `involves/has_conversation` 字段 —— 投资建议的真实措辞在对话里,不在行动计划里。
   仿照 `behavior_follow.py` 的升维/降维对照,把行为样本从行动计划换成对话文本;
2. **拉长观测窗/改锚**:对齐度现在用 2–3 词的维度名做锚,灵敏度有限;
   用治理文档里的完整维度描述语重打分,窗口拉到 24h;
3. **两张人工标注表**(等你/专家,各约 30 分钟):
   `results/analysis/router_review/router_review_sheet.csv`(104 条 issue:
   路由合适?风险等级?是否行为句)+ `reflection_review/reflection_review_sheet.csv`
   (24 条反思:八维覆盖/切题度/忠实度)。标完 `--score` 即得第一批人工一致率。

### 方向 B:GTC 演示线

1. **C 线 fired 镜头**(演示最大缺口):给场景加一条含正面事件的 Timeline C
   (`scenario.yaml` 的 `timeline.lines` + `branch_map`),让"条件触发→自动建仓→
   后果→反思"完整发生一次。数据管线已就绪;验收 = 一条 C 记录
   `condition_monitor` 出现 `fired=true` 且 `state_history` 显示建仓。
   提示:`price_below` 类条件在现有价格曲线($27.40)上能自然触发;
2. **演示脚本**:B(AI 对但 Ethan 错失)→ A(稳健却被满仓亏损)→ C(条件化严格
   执行)三幕叙事,配每幕的面板与记录编号(素材:C-1630/A-0958/B-1658);
3. **部署清单**(若需公网):`EMBED_ALLOW_ORIGINS` 白名单、只读反代、HTTPS;
4. 10 家真实公司数据(等数据侧):到达后先过隔离探针(`test_isolation.py` 套路)
   与 anti-leak,再录新样本。

### 方向 C:工程收尾(结构债,都不大)

1. **`_providers.py` 真注入化**(抽包增量 3,半天):把 4 个延迟解析
   (reflection/orchestrator/mavis_provider/场景路径)改为调用方参数注入,
   删掉过渡 seam 文件 → 包完全独立;case01 侧 shim 自动绑定,调用方无感;
2. **测试去 shim**:12 个测试文件逐个从 `case01.injector.*` 切到
   `mavis_case01_injector.*`,然后删 14 个 shim(设计稿附录的增量 4);
3. **case02_minimal 充实**:现在是 stub;做成第二个完整场景,"新增场景 = 写 YAML"
   的通用性主张才有第二个实证;
4. **反思质量评分接入 M4**:`reflection_review` 框架直接复用为专家打分界面。

### 方向 D:LoRA 线(等数据,Runbook 已写)

标记到达后按 `LoRA预准备_20260924.md` 的 5 步走。两个设计要点先记住:
- correct 样本只教格式风格,对齐方向在 DPO 对里——配比按此设计;
- 上线前过全套回归(冲突检测/一致性戳/反思质量门)+ 双模型行为对比。

---

## 三、坑清单(都实测踩过,别再踩)

- **写端点 JSON-only**:5010/8060 的 POST 必须带 `Content-Type: application/json`,
  否则 415——是特性不是故障;
- **C 线 preset 的 consistency=unknown / quality=unverified 是正常口径**,
  serve 默认列表照常返回;
- **shim 是 sys.modules 自替换**:monkeypatch/reload 语义全等;但**别在 shim 文件里
  写逻辑**——它是待删的过渡层;
- **`_providers.py` 是包内唯一允许出现 case01 字样的文件**(守卫测试在盯);
- **GoalScorer 死后端会分钟级空转**:分析工具已加 2s 预检探针;在 mavis 侧新写
  长循环调 embedding 的代码,先想清楚死后端行为;
- **mavis 测试已密封**(`_NullScorer`),别在测试里真调 Ollama;
- **文档状态测试会卡红 PR**:新 md 必须带状态块 + 登记进 `docs/文档索引.md`,
  且禁止个人称呼(用角色表述);
- **临时脚本用完即删**(本会话零残留,`git status` 可验证)。

## 四、留给用户的决定(下一棒不要自作主张)

- 两条 C 记录(1609 本地 Router / 1630 nemotron)留哪条当演示主力;
- 远端旧分支 `origin/feat/case01-injector`(与 main 同点)是否删除;
- LoRA 训练环境:本机 8GB 勉强档 vs 云端;
- CUDA torch 安装(会改本机 Python 环境,装前确认)。

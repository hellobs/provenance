# 转接信息 · 给下一棒 AI

> **状态**:已被取代(由 `转接_给下一棒_20260926.md` 接替;本文的方向/任务清单
> 中的阶段 3 与 LoRA 预准备均已完成,以新版为准)
> **最后核对**:2026-09-24
> **说明**:写于 2026-09-24 十三轮深度自检收尾。上一份权威交接是
> `转接_给下一棒_20260918.md`(§14 起的"不允许静默"等铁律仍然全部有效),本文只写
> **那之后的变化 + 你可以直接开工的事**。两份配合读。

---

## 一、2026-09-24 这天发生了什么(现状基线)

**测试**:四套全绿 —— 仓库根 117 / case01+case_engine 552 / mavis-vizkit 32 / mavis 167。
一条命令参考:

```
cd provenance
python -m pytest tests case01/tests case_engine/tests ../packages/mavis-vizkit/tests -q
```

**安全收口(全部已推送)**:
- 5002(契约服务)CORS 从 `*` 收紧为 `live.netguard.resolve_embed_origins` 白名单,
  平台跨源取数显式设 `EMBED_ALLOW_ORIGINS`;
- 5010 三个写端点(goals / undo-intervention / reflections/mark)加了 JSON-only
  Content-Type 防护(与 vizkit restart 同口径),空/坏 JSON 不再 500;
- 8060(config_tool)12 个写端点统一 `_json_body` 容错;
- git 全历史扫过:无密钥、`.secrets.json` 从未入库。

**LLM 接线(重要变化)**:
- OpenRouter 默认模型 **minimax-m3:free 已死**(官方下线),换为
  `nvidia/nemotron-3-ultra-550b-a55b`(`case01/agents/llm.py` 与 `case_engine/llm.py` 默认值);
- key 在 `case01/.secrets.json`(gitignored),当前 key 已验证可用;
- nemotron 是 reasoning 模型、输出啰嗦:`run_router` 上限已 2048→4096,
  若发现 issues=0 先查是否又被截断(manifest_warnings 会如实记录);
- 外部模型挂掉时**记录不再丢**:pipeline 会照写盘、失败写进 manifest_warnings,
  本地 qwen3(Ollama)随时可做降级 Router。

**C 线(问题 1 的现状)**:
- `PLAN_PROMPT` 已硬性要求 trigger.keywords 用中文(时间线事件是中文);
- 两条新 C 线记录可对照:`260924-live-case01-mavis-C-1609`(本地 Router)与
  `C-1630`(nemotron Router,4 条 issue);
- 条件监测链路完整:建议→中文关键词机检→逐日监测→不触发不建仓;
  Timeline A 全是否定事件,**不触发本身是正确行为**(否定词防线实测有效)。

**数据卫生**:
- 6 条 2026-09-19 早间 B 线记录(T0 缺传闻事件)已按用户决定**删除**,
  现存 24 条(21 默认可见 / 3 excluded),无悬空引用;
- `问题清单_20260925.md` 已更新实际状态(4 修 1 缓解 1 待外部),别再照它重做已修的活;
- mavis 可安装元数据已刷新到 1.3.0(`pip install -e` 过)。

---

## 二、下一棒可以直接往下做的事(按建议优先级)

1. **Branch C「条件真实触发」镜头**(演示价值最高的缺口):
   现在没有任何一条 C 记录的 `condition_monitor` 出过 `fired=true`。给 scenario
   加一条含**正面事件**的 C 专用时间线(或复用 B 线事件),`branch_map` 把 C 指过去,
   重跑 preset C。验收:一条 C 记录 `fired=true` 且 `state_history` 显示建仓。
   注意:不建议硬编码触发词——让 Investment AI 自然给出条件;`price_below` 类条件
   在下跌时间线里会自然触发,是条捷径。

2. **外部 Router 口径统一(可选)**:用 nemotron 对既有 A/B 成品
   `--router-only <path> --external-router --router-write`(自动留 .bak),
   让全部记录的 issues 出自同一模型口径。注意确认每条 .bak 都在。

3. **治理平台侧 M4(Expert Review)联调**:5002 数据面早就绪(21 条可见、
   quality/consistency/branch_source 都在),对方开工即可接。对接文档:
   `GTC平台对接说明` 与 openapi 契约(契约与实现已核对一致)。

4. **10 家真实公司 Financial Data**:数据一到先过隔离探针与 anti-leak 校验
   (沿用 `test_isolation.py` 的套路),再录 A/B 线新样本。

5. **阶段 3 迁移【已完成 2026-09-24 晚】**:实现迁至 `packages/mavis-case01-injector`
   (world + injector 九模块 + llm/consistency/atomicio 收编),provenance 侧经
   `sys.modules` 自替换 shim 兼容(调用方零改动);reflection/价格常量/mavis_provider
   经 `_providers.py` 过渡 seam 延迟解析,收尾 = 删该文件改真注入(设计稿附录)。

6. **mavis 打 tag【已完成 2026-09-24】**:v1.3.0 在更早的 idle_text 提交上已存在,故按 semver 打了
   `v1.3.1`(c458a15,config_tool 安全面补丁),main 与 tag 均已推送。

7. **GTC 口径 docx**:等研究侧确认后把拟稿贴入(`GTC侧文档口径补充_拟稿_20260918.md`)。

---

## 三、给下一棒的坑清单(都实测踩过)

- **写端点有 JSON-only 防护**:脚本打 5010/8060 的 POST 必须带
  `Content-Type: application/json`,否则 415——这是特性不是故障;
- **C 线 preset 的 consistency=unknown / quality=unverified 是正常口径**
  (T0 方案混合),serve 默认列表照常返回,别当 bug 修;
- **两套文档状态测试会把 PR 卡红**:新 md 必须带 `> **状态**:`/`> **最后核对**:`
  且登记进 `docs/文档索引.md`;文档里禁止个人称呼(用角色表述);
- **改 `live/netguard.py` 的默认白名单要同步** vizkit 插件里的同口径默认值
  (两边注释都写了"改一处要改两处");
- **`case_engine/__main__.py` 有守卫了**,但 import 别的 `__main__` 模块前先看一眼;
- 临时自检脚本用完即删(本会话产出的已全清,`git status` 干净可验证)。

---

## 四、留给用户的决定(下一棒不要自作主张)

- mavis 打 v1.3.0 tag(上面第 6 条);
- 演示口径:两条 C 记录留哪条当主力(1609 本地 / 1630 外部)还是都留;
- 是否清 `case00/scenario/` 下的 .bak 备份(加载器已验证免疫,纯占磁盘;
  用户原话"不管")。

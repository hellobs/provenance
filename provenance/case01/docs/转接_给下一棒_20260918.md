# 转接信息 · case01 injector（给下一棒 AI）

> 写于 2026-09-18。你已经知道背景，这里只写**当前精确状态 + 别踩的坑 + 下一步**。
> 本文件本身就是交接清单，读完就可以接着干。

## 0. 一句话现状

case01 归入 mavis 的整合已过阶段 1/2/3（验收四项全过），产物与文档就绪；
阶段 4 的"替换旧引擎 / 让平台换样本 / 发通知"是留给人的拍板项，**不要自己推进**。
本轮（09-18）补完了隔离实测与端到端映射复核，修了 3 个真问题，3 次提交已推送。

## 1. 仓库与分支（精确）

provenance（代码在 `provenance/provenance/`，注意双层目录）

- 路径：`D:\zzr\provenance`
- 分支：`feat/case01-injector`，顶端 `915f56c`，**已推送，未推送 0**
- 本轮三个提交：`0df4f7a`（action 收敛为文本）、`5b59660`（隔离验证三层 + C 线 timeline 修正）、`915f56c`（隔离实测落数 + 文档口径）
- **落后 `origin/main` 1 个提交**（`7ae216f` Leo 的本地 HF 权重 + viewer），领先 32 → 合并前要 rebase/merge，尚未做
- 归档标签：远端已有 `archive/case01-old-engine`（指向 `d41cdec`，旧引擎冻结点）

mavis

- 路径：`D:\zzr\mavis`
- 分支：`feat/generic-injection-hooks`，顶端 `7ba1033`，已推送
- 相对 `origin/main`：**领先 2、落后 0**（可以直接合并，无需 rebase）
- 远程：`github.com/hellobs/mavis`

GTC 侧（`D:\zzr\GTC`）**不是 git 仓库**，里面 0904doc 的 `.docx` 是研究侧交付件，
未经人工确认不要改（改了无法回滚）。

## 2. 环境与怎么跑起来

- Python：`D:\zzr\provenance\provenance\.venv-live\Scripts\python.exe`
- LLM：本地 Ollama `127.0.0.1:11434`，`qwen3:4b-instruct-2507-q4_K_M` + `qwen3-embedding:0.6b-q8_0`
- mavis 是 **editable 安装**（`__editable__.mavisframework-1.0.0.pth` → `D:\zzr\mavis`）
- 所有命令在 `D:\zzr\provenance\provenance` 下执行：

```
# 实时可视化（小镇 Phaser 前端，端口 5010，跑完保持 30 分钟）
python -m case01.vizkit.live_run --branch B --port 5010 --hold 1800 \
    --out case01\runs_injector\live-B\raw.json

# 原始记录 → case01 兼容 run.json（含 Reflection/Router）
python -m case01.injector.pipeline --branch B --from-record <raw.json> --reflect \
    --out case01\runs_injector\live-B\record-B-mavis.json

# 隔离验证实测（默认真实运行 + 对抗模式：整条时间线全进池）
python -m case01.tools.isolation_probe --branch B \
    --out case01\runs_injector\isolation\probe-B.json

# 静态审查页（离线看某次 run）
python -m case01.viz

# 测试
python -m pytest case01\tests -q          # 174 passed
cd D:\zzr\mavis && <同一个 python> -m pytest tests   # 91 passed
```

- 注意：给 mavis 跑 `pytest` 时不要在 pyproject 的 `-q` 上再加 `-q`（`-qq` 会吞掉汇总行）。
- `case01/runs_injector/` 自带 `.gitignore`（自忽略），跑出来的记录不会污染仓库。
- 实时服务**当前已停止**（上一轮 hold 到 14:20 结束）；要给人看就照上面第一条命令重启。

## 3. 锁定的架构决定（别推翻）

- case01 = **作用于 mavis 的一套事件参数约束**；世界事实由 injector 状态机决定，mavis 只管角色表达与交互。
- 只放 2 个角色（Investment AI / Ethan Lin）；不挂 governance/consequence。
- 1 个节点 = 1 步；关键节点若没交互，在节点内重试（`--max-retries`）。
- 事件通道：只释放当前节点的 story 事件（`condition.type=case01_node`）；**事件进记忆，步级临时状态只进提示词**。
- mavis 侧改动是**三处纯新增、默认关闭**：`Simulator(external_state=)`、`Simulator(interaction_request=)`、agent 配置 `role_directive`。**已经用满三处，若还需第四处，先停下来复审**（停手判据写在设计说明 §2）。
- 记录与 case01 `run.json` **schema 等价**（顶层键齐全），文本内容可不同，5002 三端点契约不变。
- 运行中干预：只保留接口，本期不实现。

## 4. 阶段 3 四项验收 —— 已全部通过

1. **字段级对比**：`record.py` 映射出全部顶层键，`compat.missing_keys` 为空；
   端到端复核（09-18）：8 turns / 12 events / 7 retrievals / 22 audit / reflection 5167 字 /
   Router 4 个问题，Router 问题字段与旧引擎逐键一致（`id/summary/field/risk/routing_reason`）。
2. **隔离验证**：三层取证。4 例单测（池子层/判定层/步级状态通道）+ 真实运行探针：
   各节点记忆条目 2/4/6/8/10/12/12 与已释放事件累计数逐一相等、非已释放条目 0；
   **60 次用后续节点原文主动检索，0 次命中未释放内容**。
3. **自发行为**：每轮 B 线 1 次非请求对话，节点不固定（09-16 在 09-03，09-18 都在 08-31），已记录。
4. **mavis 改动审查**：三处纯新增，全量 91 例通过，未触发停手规则。

## 5. 本轮修掉的 3 个真问题（都已有测试兜住）

1. `branch_action.timeline` 原来照抄分支号，**Branch C 实际走 Timeline A**
   （旧 `demo-3` 实测与 `BRANCH_TO_TIMELINE={A:A,B:B,C:A}` 都是 A）→ 已按映射取值并补测试。
2. 前端 `action` 显示成 `"描述 / 开始时间 / 时长"`——mavis 的 `AgentState.action` 是
   `{event:{describe..}, start, duration}`，已在 `vizkit.events.as_text()` 里改成只取内层描述。
3. 隔离探针第一版判据错误：mavis 检索是 top-k 相似度、**没有相关度阈值**，
   "有返回"不等于"泄漏"，导致 60/60 假阳性。已改为"返回文本里真的出现未释放事件原文才算泄漏"；
   同时快照改为读**全量**事件记忆（`retrieve_events()` 会被 `retention=8` 截断，窗口外的条目会漏）。

## 6. 已知隐患（按重要性）

1. **版本号＝同一个 `1.0.0` 对应两份不同内容**。`mavis/pyproject.toml` 没 bump，但三处钩子是
   同版本号下加的；`mavis/dist/mavisframework-1.0.0.tar.gz`（2026-08-25）解包后**不含钩子**；
   而 `provenance/requirements.txt` 写的是 `mavisframework==1.0.0`。
   本地能跑只因为 `.venv-live` 是 editable 安装 → **按 requirements 装出来的环境会 `TypeError`**。
   修法必须三步一起：bump（建议 1.1.0）+ 重出 sdist/wheel + 更新 pin。**待人工决策，未做**。
2. provenance 分支落后 `origin/main` 1 个提交，合并前要 rebase。
3. 记录里**不含模型身份**（后端/权重/dtype/device），HF 与 Ollama 产生的 run 在记录上不可区分（阶段 0 决定不做）。
4. CI 仍不覆盖 `case01/tests`，只能靠本地跑（阶段 0 决定不做）。
5. `GTC/docs` 与 0904doc 的口径同步只做了一半：0908 速览已改，
   **01/04/05 是成稿待贴**（`case01/docs/GTC侧文档口径补充_拟稿_20260918.md`），要人确认后贴入 `.docx`。
6. 文档里的秒数有两个口径：**整跑墙钟**（含场景构建与 Reflection）vs 记录里
   `injector.summary.elapsed_s`（只是节点耗时之和）。三线记录里是 302.6/286.4/302.8 秒，
   加构建与反思才是 437/390/417 秒。引用绝对秒数必须写口径，同日复跑会浮动到 253.8–350.5 秒。

## 7. 待人工拍板的关口（**不要自己决定**）

1. 是否正式替换已冻结的旧引擎、让平台换样本。
2. 是否现在做 mavis 版本 bump 三步。
3. 平台侧通知是否正式发出（草稿：`case01/docs/通知平台侧_换样本_20260917.md`）。
4. 01/04/05 口径拟稿是否贴入 GTC 侧文档。
5. Branch C 真实 demo 是否重录（当日 C 为 `wait` 且未触发，修复不影响结果，**可复用**）。

## 8. 工作区体检（本轮新开）

对 case01/mavis 之外的仓库做了只读体检（Unity 工程 / 治理平台侧 / 工作区仓库总览）。
原则：**先出问题清单，不动手改**。Unity 结论见 8.1；治理平台侧结论见第 11 节；
工作区仓库总览那一份（子任务 `2d4df6cb-1fab-4673-b5fd-ae3faf91bc7c`）未回收。

### 8.1 Unity 仓库（本轮已核实）

`D:\zzr\Multi-Model-AI-Visualization-and-Interactive-Simulation-Platform\`

- 方向性事实：`D:\zzr\GTC\docs\战略调整_放弃Unity统一Web承载.md`（2026-08-30，与仝牧共识）
  已把 **Unity 仓库冻结/暂停**、把 provenance(Web) 定为主战场。仓库最后一次提交
  `782b8b6`（2026-08-30）就是这次冻结，`CLAUDE.md` 第 7 行有 v0.4 冻结横幅、
  第 424 行变更表也记了 —— **这两处是一致的，不需要再改**。
- 真正的问题有两个：
  1. `docs/DEVELOPMENT_PLAN.md` **没有任何冻结标记**，通篇仍写成"当前执行基准、
     完成一项勾一项"，`README.md` 同样没提冻结。只看这两个文件的人会以为工程还在做。
     严重度：中。
  2. 工作树有 **1 个未提交改动**：`Assets/_Project/Scripts/Agents/AgentMovement.cs`
     （+33/−1，内容是"演示增强：空闲游走 + 强制快速移动"）。这是在冻结前留下的半成品，
     既不提交也不丢弃。严重度：中（要么挂到分支提交，要么明确丢弃，别悬着）。
- `Assets/_Project/` 骨架与美术资源（Agents 立绘、Tilesets、sprite.json 等）确实存在，
  说明冻结前 Phase 0 做了一部分。

## 9. 工作约定（这个工作区里一直这么干）

- **每做完一版就 commit 并真的 push**，不要攒着；提交信息用中文、说清现象与根因。
- 文档用中文；**不要用比喻、不要用表格**（用户明确要求过），结论要简洁。
- 消息契约以 `frontend/templates/main_script.html` 的**实际解析**为准，它是权威；
  契约变了要同步 `case01/vizkit/events.py` 的注释与 Unity 工程 CLAUDE.md §7/§8。
- 不要改 mavis 的现有行为语义；三处钩子已用满。
- 不要动 GTC 侧别人的 `.docx`，也不要假设 `D:\zzr\GTC` 有版本控制。
- 起后台服务用 `Start-Process ... -RedirectStandardOutput`，不要用管道去截断（会把进程弄死）；
  杀服务先按 `live_run` 匹配进程再杀。
- PowerShell 里带引号的长中文提交信息要写进临时文件再 `git commit -F`。

## 10. 建议的下一步顺序

1. 先把本轮 3 个提交确认在工作树上（`git log --oneline -3`，应见 `915f56c`）。
2. 收体检结果（第 8 节），把问题清单交给用户排序，再决定动不动手。
3. 用户点了哪一个关口就做哪一个；mavis 版本 bump 若要做，先保证没有实时运行在跑（会重装 editable 元数据）。
4. 要给人演示时再按第 2 节第一条命令起实时服务，并确认 `/health` 返回 ok 再让人打开页面。

---

## 11. 治理平台侧体检结论（已完成，含我对子任务一条结论的更正）

仓库 `D:\zzr\GTC-AIGVD-Governance-and-Decision-Making-Platform\`（Next.js/Cloudflare + FastAPI），
分支 `main`，**领先 origin/main 1 个未推送提交** `563c320`（2026-08-31，Ruige），
此后再无提交（backend 的 .venv 在 9/08–9/11 被动过，但没留下提交）。

- **最严重**：全仓库**零代码**调用 5002 的三个只读端点或 `/viewer`。唯一耦合是
  `app/page.tsx:383-401` 的一个 iframe 指向 **5001** 的 `/embed/scene`（另有 `/`、
  `/embed/goals`、`/embed/explain`）。也就是说平台现在嵌的是"实时画面"，
  没有任何"已完成 run 的治理数据"入口。
- 04/05 要求的专家审核链路（任务队列 / 问题详情 / Approve·Edit·Reject / Full Context /
  Disputed 追加审核 / 材料池状态）在代码里**零实现，也没有占位骨架**——属于未开工。
- 配置与文档：`app/page.tsx:383` 读 `NEXT_PUBLIC_PROVENANCE_URL`，而 `.env.example` 里
  只有 `NEXT_PUBLIC_GTC_API_URL` → 嵌入地址只能吃代码里的硬编码兜底；
  `README.md` 的后端启动写 `.venv/bin/python`（POSIX，本机是 Windows），
  与 `backend/README.md` 的 `uv sync --extra dev` 两套步骤不一致；`backend/uv.lock` 未提交。
- 已核实无问题：无真实密钥入库（只有 env 变量名）；无 TODO/注释掉的实现；`.gitignore` 正确忽略 `.env*`。

**更正子任务的一条结论（重要）**：子任务报"`/viewer` 在 5002 不存在、引擎文档需更正"——
**这个结论是错的**。`/viewer` 确实存在，只是在 **`origin/main`** 上（Leo 的 `7ae216f` 用
`app.mount("/viewer", StaticFiles(RUNS_HTML_ROOT, html=True))` 挂的），而我这条分支
落后 main 一个提交，所以工作树里没有它（子任务 grep 的是我的工作树）。
**真实风险不是文档写错，而是分支没对齐**：对外承诺的契约（含 `/viewer`）在我这条分支上不成立。
这正好落回第 6 节第 2 条那个 rebase 项——在合并 main 之前，不要拿这条分支去对外联调。

引擎侧要补的契约（子任务提出，我核对后认可）：

1. `risk` 实测是小写 `high/medium/low`（旧引擎 demo-1/2/3 也是小写），
   而 `Governance平台对接说明.md:62` 写成 "Low / Medium / High" → 口径不一致，
   应写明以小写为准，或约定平台侧忽略大小写。
2. issue id 是 run 内序号（`issue-1..N`），跨 run 不唯一 → 契约里要写死
   平台侧主键为 `(run_id, issue_id)`，否则建单会错配、不幂等。
3. run.json 顶层比合同多出 `summary` / `injector` / `compat` 三个键，应显式标注
   "平台不读"；`injector` 里有 Branch 实验元信息与 dialogue，**绝不能进专家视图**。
4. 材料池若要按模型/后端筛选，需要补 `model/dtype/device`（阶段 0 已决定不做，需重新评估）。
5. 5001 的 `/embed/*` 目前没有契约（尤其 `/embed/reflections`：引擎自述是"反思标记→LoRA 线
   数据入口"，平台已经在嵌 5001 却完全不知道有这个面板）——契约文件只冻结了 5002。

以上按用户要求**只列清单，未动手改**。

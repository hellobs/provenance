# 任务:把 vizkit 抽成独立插件包(mavis 插件化 · 阶段 1)

> **状态**:历史存档
> **最后核对**:2026-09-22
> **说明**:任务派发单:任务已完成,留作过程记录
> 这份是给另一个 AI 的完整任务书,可直接整段粘贴执行。
> 目标产物:`packages/mavis-vizkit/`(独立可分发包,零 case01 词汇、运行时不依赖 mavis)。

## 0. 一句话任务

把 `provenance/provenance/case01/vizkit/` 从 case01 里**抽成独立包**,使它成为
"任何 mavis 使用方都能装的观察者/可视化插件集";case01 改为依赖它。
**本阶段不动 mavis 仓**,也不要把 case01 逻辑搬进 mavis。

非目标(明确不要做):

- 不要修改 `D:\zzr\mavis` 的任何文件(那是阶段 2)。
- 不要把 case01 的 injector / 业务语义搬进新包,也不要把它们搬进 mavis。
- 不要新建 git 仓库(先放在 provenance 仓里,后续要发布再迁)。
- 不要顺手重构 case01 的注入/驱动逻辑,本阶段只做"抽出 + 解耦 + 接线"。

## 1. 先读这三份(按顺序)

1. `D:\zzr\provenance\provenance\case01\docs\转接_给下一棒_20260918.md`(当前状态、命令、纪律)
2. `D:\zzr\provenance\provenance\case01\docs\case01_触点白名单.md`(允许碰 mavis 的边界)
3. `D:\zzr\mavis\docs\tutorial-extension.md`(mavis 的扩展面;本阶段不修改它)

## 2. 环境与纪律

- 仓库:`D:\zzr\provenance`(分支 `feat/case01-injector`),代码在子目录 `provenance/provenance/`。
- Python 一律用仓库自带 venv:`D:\zzr\provenance\provenance\.venv-live\Scripts\python.exe`
  (系统 python 会 `ModuleNotFoundError: mavisframework`)。
- 跑 pytest 不要叠加两个 `-q`(pyproject 已有 `addopts = -q`,`-qq` 会吞掉汇总行)。
- **验证"已安装的包"必须在无关目录里跑**:在 `D:\zzr\mavis` 或包源码目录下,
  `import` 会命中工作树源码而不是 site-packages,会得出假结论(这个坑上一轮踩过)。
- 后台服务用 `Start-Process ... -RedirectStandardOutput` 起;不要用管道截断(会把进程弄死)。
- 提交信息用中文、写清现象与根因;带引号的长信息先写临时文件再 `git commit -F`。
- 文档用中文,**不要用表格、不要用比喻**;结论简洁。
- 每完成一版就 `git commit` 并 `git push origin feat/case01-injector`,不要攒。

## 3. 现状(已核实,带行号)

`case01/vizkit/` 的骨架**已经不 import mavis**,只消费"事件字典"这套协议键名:

- `__init__.py`:`Visualizer`(基类,`on_event`/`on_record`/`close`)、
  `register(name, factory)` / `names()` / `create(name, **kwargs)`、`Fanout`(含逐插件错误隔离)。
- `events.py` 事件构造与归一化:`init_event` / `time_event` / `agent_event` / `chat_event` /
  `story_event` / `snapshot_event` / `as_text` / `normalize_record` / `events_from_record` / `live_hooks`。
- `replay.py` 回放:`record_needs_fallback` / `replay_record` / `load_record` / `main`。
- `plugins/`:`ConsoleVisualizer` / `ReportVisualizer` / `TownVisualizer` / `LiveVisualizer`。
- 测试:`case01/tests/test_vizkit.py` + `case01/tests/test_vizkit_live.py`,共 20 例。

要解耦的四处(这是本任务的全部难点):

1. `plugins/live.py:26-30`:`FRONTEND_DIR` 从 `plugins/live.py` 往上数四层推出
   `provenance/provenance/frontend` → 绑死本仓目录布局。
   (`static_root` / `template_dir` 已经是可注入参数,问题只在那两个默认值。)
2. `plugins/town.py:21-29`:`ROLE_TEXTURE_ALIAS = {"Investment AI": "AI Advisor",
   "Ethan Lin": "Mr. Zhou"}` 与 `DEFAULT_SCENARIO`(指向 case01 的 scenario 目录)
   是业务词汇与业务路径。
3. `events.py:101-113`:`normalize_record` / `events_from_record` 里兼容
   `injector.nodes` 这一**记录布局**的写法,含 `injector` 这个词。
4. `vizkit/live_run.py`:CLI 里 `from ..injector.bridge import ...` / `default_nodes(...)`,
   把 case01 的驱动逻辑与可视化混在一个模块里。

## 4. 目标形态

目录(建议):

```
D:\zzr\provenance\packages\mavis-vizkit\
  pyproject.toml          # name = "mavis-vizkit", version = "0.1.0", 零运行时依赖
  README.md               # 说明它消费的事件协议与四个插件
  src\mavis_vizkit\
    __init__.py           # Visualizer / register / names / create / Fanout
    events.py             # 事件构造与归一化(不含任何业务词)
    replay.py             # 回放驱动
    plugins\
      __init__.py         # 导入即注册四个插件 + load_entry_point_plugins()
      console.py report.py town.py live.py
  tests\                  # 原有 20 例迁过来 + 本阶段新增的边界测试
```

要求:

1. **零业务词汇**:`src/` 里不出现 `case01` / `Case01` / `injector` / `Investment AI` /
   `Ethan Lin` / `HCM`(注释里也不要);别名表、scenario 目录、前端资源根一律由调用方传入。
   没有传就报清晰的错,不要回退到任何本仓路径。
2. **运行时不 import mavis**:包只依赖"事件字典"的键名。
   在 `README.md` 里把消费的键名列成契约;并加一条**漂移测试**:
   测试里 `from mavisframework.runtime.protocol import ...`(仅测试期 dev 依赖)
   断言这些键名仍然存在。这样既没有运行时耦合,又能第一时间发现协议变了。
3. **注册与发现**:`mavis_vizkit.plugins` 导入即注册四个插件;
   另提供 `load_entry_point_plugins()` 读取入口点组 `mavis_vizkit.plugins`
   (显式调用才生效,默认不自动发现)。
4. **布局兼容改参数**:`normalize_record(record, nodes_key="nodes")` 之类,
   由调用方给"节点列表挂在哪个键下";包内不出现 `injector` 字样。
5. **case01 侧只留壳**:`case01/vizkit/live_run.py` 保留(命令
   `python -m case01.vizkit.live_run` 是既有文档里的命令,不能失效),但改成
   薄 CLI:import `mavis_vizkit` 与 case01 的 injector,把 roles / scenario / 前端资源根
   作为参数传进去。`case01/vizkit/__init__.py` 可留一个**只做 re-export 的兼容壳**
   (docstring 写明"实现已抽到 mavis-vizkit"),但**不许再留任何逻辑**。
6. **依赖接线**:把新包以 editable 装进 `.venv-live`;
   `provenance/requirements.txt` 加一行 `mavis-vizkit==0.1.0` 并注明
   "本仓内 packages/mavis-vizkit,editable 安装";`.github/workflows/ci.yml` 在跑
   `case01/tests` 之前先 `pip install -e packages/mavis-vizkit`。

## 5. 实施步骤

1. 建包骨架 + `pyproject.toml`(setuptools,`src` 布局,`requires-python >=3.12`)。
2. 搬 `events.py` / `replay.py` / `__init__.py`(改掉第 3.3 条的布局写死)。
3. 搬四个插件,按第 4.1 条把别名表、scenario、静态资源根参数化。
4. 把 20 例测试搬到新包 `tests/`,分成"通用"与"需要参数化 fixture"两类;
   需要 case01 角色的用例改成调用方传别名/scenario(临时目录造最小 scenario)。
5. 新增边界测试:①`src/` 零业务词汇;②`src/` 不 import mavisframework;
   ③协议键名漂移测试;④`create("town")` 不传别名时报错而不是猜。
6. 改 case01 接线(第 4.5 条),删掉 `case01/vizkit/` 里除薄壳以外的实现。
7. 更新文档:`case01/docs/转接_给下一棒_20260918.md`(命令与目录变化)、
   `case01/docs/case01_触点白名单.md`(新包不在 mavis 触点里,说明它是纯消费者)、
   `case01/README.md` 的 vizkit 段。
8. 全量回归 + 提交推送。

## 6. 硬验收标准(逐条跑,把输出记下来)

**A. 包内边界**

```
rg -n "case01|Case01|injector|Investment AI|Ethan Lin|HCM" D:\zzr\provenance\packages\mavis-vizkit\src
rg -n "^\s*(import|from)\s+mavisframework" D:\zzr\provenance\packages\mavis-vizkit\src
```
两条都必须**零命中**。
本机 `rg` 不在 PATH,用 PowerShell 等价写法:

```powershell
Select-String -Path (Get-ChildItem D:\zzr\provenance\packages\mavis-vizkit\src -Recurse -Filter *.py).FullName `
  -Pattern 'case01|Case01|injector|Investment AI|Ethan Lin|HCM'
Select-String -Path (Get-ChildItem D:\zzr\provenance\packages\mavis-vizkit\src -Recurse -Filter *.py).FullName `
  -Pattern '^\s*(import|from)\s+mavisframework'
```
(注意 `Select-String` 需要文件列表;不要用 `-Recurse`,它不是 `Select-String` 的参数。)

**B. 包自测**

```
cd D:\zzr\provenance\packages\mavis-vizkit
<venv python> -m pytest tests -q          # 全绿(≥ 原 20 例 + 新增边界例)
```

**C. case01 与外层回归(必须与改动前一致)**

```
cd D:\zzr\provenance\provenance
<venv python> -m pytest case01\tests -q   # 期望 196 passed(或更多,不能少、不能红)
cd D:\zzr\provenance
<venv python> -m pytest tests -q          # 期望 23 passed
```

**D. 实时可视化端到端冒烟**

```
cd D:\zzr\provenance\provenance
<venv python> -m case01.vizkit.live_run --branch B --port 5011 --nodes 1 --hold 40 \
    --out case01\runs_injector\smoke\raw.json
# 另开一条命令:
curl http://127.0.0.1:5011/health        # 期望 {"status":"ok",...}
```
若 Ollama 不可用,至少跑等价的 `TestClient` WebSocket 测试并说明原因。

**E. 没有把耦合藏起来**:`git diff --stat` 里不应出现 `D:\zzr\mavis` 的任何路径;
新包里不应出现"为了兼容而临时写死"的路径常量(有就在报告里点名)。

## 7. 回报格式

1. 变更清单(新增/移动/删除的文件,一句话说清每个)。
2. 验收证据:A–E 五条的实际命令与输出摘要(数字要对得上)。
3. 解耦结果:四处耦合各自怎么解的,有没有留下例外(有就明说)。
4. 未决问题与建议:特别是"阶段 2 需要 mavis 提供什么插件接口"——
   基于本阶段踩到的实际情况给建议,不要泛泛而谈。
5. 下一棒需要知道的坑(新增的或仍然存在的)。

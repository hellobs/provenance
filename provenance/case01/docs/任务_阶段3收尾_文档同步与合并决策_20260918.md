# 任务:阶段 2/3 收尾(文档同步 + 补真实链路回归 + 合并决策材料)

> **状态**:历史存档
> **最后核对**:2026-09-21
> **说明**:任务派发单:任务已完成,留作过程记录
> **最后核对**:2026-09-21
> **说明**:任务派发单:任务已完成,留作过程记录
> **最后核对**:2026-09-21
> **说明**:任务派发单:任务已完成,留作过程记录
> **最后核对**:2026-09-21
> **说明**:任务派发单:任务已完成,留作过程记录

> 给另一个 AI 的完整任务书,可整段粘贴执行。
> 前置:A/B/C 三项已通过独立验收(见 §2)。本次是收尾,不含新功能、不动行为。

## 1. 先读

1. `case01/docs/任务_阶段3_插件面接线与抽包设计_20260918.md`(你刚做完的那份)
2. `case01/docs/任务_阶段3_抽包设计稿_20260918.md`(你写的设计稿)
3. `case01/docs/case01_触点白名单.md` 与 `case01/docs/case01_可插拔可视化设计.md`
4. `D:\zzr\mavis\docs\tutorial-extension.md` §7/§8

## 2. 独立验收结论(哪些已通过,不要重做)

四套测试复跑一致:mavis **122** / mavis-vizkit **25** / case01 **204** / 外层 **23**。
mavis `feat/plugin-surface@dca75b6` 领先 main 2 个提交、未推送、main 仍为 `8f12919`;
相对 main 只改了 8 个文件,未碰 pyproject/dist,本地无新构建产物。

- 任务 A 三项全部到位(两份 README 的插件面与多订阅、`plugin.py` 分类型准确描述、只读约定)。
- 任务 B:特性探测三要件、适配器只在 case01 侧、插件面下不覆盖 `chat_callback`、回退路径
  带守卫并在 `close()` 清回;`close()` 里 `fanout.close()` 仍在早退之前,dry-run 无插件路径未破。
- 任务 C:设计稿 §0 的耦合事实我逐条与代码核过,全部属实(含 `worldfacts.py:60`
  `from .. import orchestrator`),`world/` 确实纯净;§1/§2/§3 有结论+理由+被否方案,
  §4 是 8 步可执行清单,§5 覆盖五类风险,全文无表格,C 只新增了这一份文档。

我另外独立验证了你报告里的两处自我诊断,都成立:

- `is` 比较绑定期程方法恒假:`x = b.m` 后 `x is b.m` → False、`x == b.m` → True。
  所以原 `close()` 那句确实永远清不掉钩子,你改 `==` 是对的。
- 收集期加载 mavis 会让 `test_dry_run_record_and_no_mavis_dependency` 红:我往
  `case01/tests/` 临时塞了一个模块级 `import mavisframework.runtime.llm` 的探针文件,
  该用例立刻 FAILED;删掉后单独跑 4 passed。你把 `VizForwarder` 改成惰性导入确实避开了。

**但有一条你没覆盖、我手工验了**:`_build_mavis` → `simulator._ensure_plugins()`(真实运行里
由 `Simulator.simulate` 触发的那次自动订阅)→ `agent_core._emit_chat_line(...)` 这条链路。
结果是通的(fanout 收到 chat_line、第二个消费者同时收到、`close()` 之后不再收到),但你的
测试只直接调 `pmgr.emit` / `simulator.emit_chat_line`,**没走这条链**;而
`test_plugin_mode_teardown_unsubscribes_chat` 在"从未订阅"时也会通过,分辨不出
"订阅过又退订"与"根本没订阅"。下面任务 A3 要求补上。

## 3. 任务 A(必做)

### A0. 推送 provenance

`D:\zzr\provenance` 的 `feat/case01-injector` 现有 2 个提交未推送(`f6ab0a1` 接线迁移、
`08fff83` 抽包设计稿),加上本任务书的提交。按既有惯例推上去:
`git push origin feat/case01-injector`,并在报告里给出推送前后的领先数。
(mavis 分支仍**不推**、不碰 main。)

### A1. 同步 `case01/docs/case01_触点白名单.md`(中)

这份文档的全部价值是"case01 碰了 mavis 的哪几行",现在它有两类失真:

**① 内容失真(阶段 2/3 之后没跟上)**:第 55 行把 `chat_callback` 列为进程级触点、
第 64-65 行写"231–233 `agent_core.chat_callback = ...` —— 白名单内的进程级钩子;
注意它是**全局单例**,同进程只能有一个消费者(与 provenance 实时服务共用时要协调)"。
这句描述的正是本次修掉的缺陷,现在只在回退路径成立。要求改成:

- 插件面存在时:对话经 `Simulator(plugins=[VizForwarder])` 自动订阅走插件总线,
  **不覆盖全局 `chat_callback`**,多消费者经 `agent_core.subscribe_chat_line` 共存;
- 仅当插件面不存在(特性探测 False,即 mavis main)才回退到"直接赋值全局钩子",
  且 `close()` 用 `==` 比较后清回;写明这条回退路径的触发条件。

**② 行号整体漂移(bridge.py 因阶段 1/2/3 变长,第三节每一行都对不上了)**:实测
`reset_game()` 217→**251**、`chat_callback` 231–233→**288**、`simulator.story` 248→**302**、
`get_agent(...).coord` 279–282→**340**、`agent.schedule.daily_schedule` 286→**347**、
`agent.make_schedule()` 287→**348**、`agent.path = []` 297→**359**、
`simulator.interactions`/`simulate` 373/378→**466/467**。
要求**整节机械重核并刷新全部行号**(含 `isolation_probe.py` 与 `conditions.py` 那两段),
不要只改我举的这几个;并把"行号会随文件变长漂移"这一点在节首注明一句
(建议标注核对日期,便于下次判断是否过期)。

**③ 补白名单缺项**:第二节(允许依赖)与第三节(当前触点)都要加上本次引入的面:
`Simulator(..., plugins=)`、`Simulator.plugin_teardown()`、以及适配器继承的
`mavisframework.plugin.Plugin`(可选依赖,探测不到就不加载)。另外测试里用到的
`agent_core.subscribe_chat_line` / `unsubscribe_chat_line` / `_emit_chat_line`
属于**仅测试使用**的触点,也请在第三节注明(注明"仅测试,不参与正式运行")。

### A2. 同步 `case01/docs/case01_可插拔可视化设计.md`(中)

第 31-36 行"两条来源都归一到这套事件"里,**在线**来源仍写
"mavis `Simulator` 的 `on_agent / on_step / on_story` 回调,加上 `agent_core.chat_callback`
的逐句对话"。要求改为反映现状:

- 在线来源有两条路:(a) 插件面存在时——`on_agent`/`on_step` 回调(记录侧与 time/snapshot)
  加上经适配器 `case01/injector/viz_plugin.py`(`VizForwarder`)从插件总线转发的
  `agent`/`story`/`chat_line`;(b) 插件面不存在时的回退——`on_story` 回调 +
  全局 `chat_callback`;
- 并在第 3 节"插件机制"里**区分两层插件**,避免读者混淆:一层是 mavis 的**通用插件面**
  (`Plugin` / `PluginManager`,`mavisframework/plugin.py`),适配器挂在它上面;
  另一层是 **mavis-vizkit 的可视化插件**(`Visualizer` / `Fanout`,`packages/mavis-vizkit`),
  适配器把总线事件转给它。写明适配器是两者之间唯一的桥,且 mavis-vizkit 仍零 mavis 依赖。

### A3. 补一条真实链路回归用例 + 强化退订用例(中)

在 `case01/tests/test_viz_plugin_wiring.py` 增补:

1. **真实链路用例**(等价于我手工验的那条):
   ```
   rec = _Recorder()
   bridge, _ = _bridge_with_fanout([rec], monkeypatch, tmp_path)
   bridge._build_mavis()
   bridge.simulator._ensure_plugins(None, {})      # 真实运行里由 simulate 触发
   import mavisframework.core.agent_core as ac
   ac._emit_chat_line("Investment AI", "真实链路")
   # 断言 fanout 收到 chat_line(即 agent_core → 总线 → 适配器 → fanout 打通)
   ```
2. **强化 `test_plugin_mode_teardown_unsubscribes_chat`**:必须在 `close()` **之前**
   先断言"经 `ac._emit_chat_line` 能收到",`close()` 之后再断言收不到。
   否则该用例无法排除"从未订阅"这种假通过。

要求:两处都用 `ac._emit_chat_line`(mavis 的单一分发点),不要改用
`simulator.emit_chat_line`(那会绕开真实订阅路径);用例结束要清理自己加的订阅者。

## 4. 任务 B(小):修设计稿瑕疵与测试死代码

**设计稿 `任务_阶段3_抽包设计稿_20260918.md` 四处**:

- L69 `是一条第可能再被 review 修订的分支` —— 衍字"第",删。
- L97 `与反射路由的窄入口(if injector 实需 run_reflection / run_router)` ——
  编辑残留的 `if`,改成 `(injector 实需 run_reflection / run_router)`。
- L194 `与 case01/runs_out 目录布局` —— **`runs_out` 这个目录不存在**(实际是
  `runs_injector`、`runs`、`runs_html`)。这句是"schema 冻结"的关键句,必须改准。
- L220 `不动钟表:` —— 比喻,换成直白表述(例如 `不改依赖方向:`)。

**`test_viz_plugin_wiring.py` 死代码**:

- L68-70 的 `import builtins` 与 `_real = builtins.__import__` 从未被使用,删。
- `env` fixture 里 `saved_cb` / `saved_subs` / `_reset` 的保存恢复与 `monkeypatch`
  重复,且 `_reset` 从未被调用。二选一:要么显式调用它,要么删掉这段、只留
  `monkeypatch` 那两行(推荐后者,顺带把 `ac._chat_subscribers` 的直接赋值也交给
  monkeypatch 管,避免污染模块级状态)。

## 5. 任务 C(只写不执行):合并决策材料

mavis 插件面要合进 main 才能开始阶段 3 抽包(这是你设计稿 §2 的结论,我认可),
而"合不合、要不要 bump 版本"是人工关口。要求写一份**一页**的决策材料
(放 `case01/docs/`,文件名自拟且标明日期),至少覆盖:

1. 影响面:分支相对 main 的 8 个文件与增删行数;纯新增/默认关闭;唯一一处既有谓词微差
   (`on_story` 守卫由真值判断改成 `is not None or self._pmgr`);对既有使用者的影响评估。
2. 精确动作与命令:建议的合并方式(可否 fast-forward)、tag 命名、**是否 bump 版本**
   (mavis 现为 `1.1.0`)——给结论 + 理由 + 被否选项。注意:bump 与不 bump 的后果要对齐
   上一轮修过的"同一版本号对应两份不同内容"那个隐患(见
   `case01/docs/mavis版本bump方案_确证_20260918.md`),不要只说"建议 bump"。
3. 合并后的基线锚点:provenance 的 CI 从 mavis main 装框架,合并后要不要加"该基线上有插件面"
   的前置断言;阶段 3 新包如何用 commit/tag 固定基线。
4. 回滚方式(revert / 重置到标签 / 分支保留),以及回滚对已依赖插件面的代码的影响。
5. 启动条件:列出"阶段 3 抽包可以开工"的前置清单(逐条可勾),并明确"合并完成前不开工"。

**明确声明**:本任务不执行合并、不打 tag、不 bump 版本、不推 main、不动 mavis 分支——
只把材料写给人工拍板。

## 6. 环境与纪律(沿用)

- 仓库:`D:\zzr\provenance`(分支 `feat/case01-injector`,本次要 push)、
  `D:\zzr\mavis`(仍在 `feat/plugin-surface`,**只提交不推**)。
- Python:`D:\zzr\provenance\provenance\.venv-live\Scripts\python.exe`。
- 跑 pytest 不要叠加两个 `-q`(pyproject 已带 `addopts = -q`);要干净汇总行用
  `-o addopts="" -q`。
- 验证"已安装的包"必须在无关目录里跑(在包源码目录或 `D:\zzr\mavis` 下 import 会命中工作树)。
- 不要在任何测试文件的**模块级** import mavis(会污染 `test_dry_run_record_and_no_mavis_dependency`
  的 `sys.modules` 断言)。
- 文档用中文,不要表格、不要比喻;提交信息中文写清现象与根因,长信息写临时文件再
  `git commit -F`。

## 7. 回报格式

1. **A0**:推送前后的领先数;`git log --oneline -3`。
2. **A1**:白名单改了哪几处(内容失真/行号/缺项),行号刷新后的对照(至少列 8 处旧→新);
   `subscribe_chat_line` 等测试触点的落位。
3. **A2**:`可插拔可视化设计.md` 改前改后各一句;"两层插件"怎么区分的。
4. **A3**:两个用例的函数名与关键断言;补测后 case01 的测试数(原 204);四套测试数字。
5. **B**:四处设计稿瑕疵 + 死代码的实际改动。
6. **C**:决策材料路径 + 五个小节各自的结论摘要(尤其"要不要 bump 版本"的结论与理由)。
7. **明确声明**:provenance 是否已推送(领先数 0?);mavis 是否仍未推送、main 是否未动、
   是否碰过 pyproject/dist/tag。
8. **下一棒要踩的坑**:新增的或仍然存在的。

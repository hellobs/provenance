# 通知：Case01 平台侧联调样本换新（mavis 新架构）

> **状态**:未发出·已过时
> **最后核对**:2026-09-24
> **说明**:从未发出;样本改名、quality/debug 默认过滤、A 线样本不自洽都发生在此文之后 —— 与 `架构总览与对接指南 §11` 合并后重发
> 提出方：实现侧（研究侧）
> 变更时间点：2026-09-17（三条新样本已落盘 `case01/runs/`）
> **命名修订：2026-09-19。** 记录名统一为
> `<真实日期 YYMMDD>-<kind>-<case>-<engine>-<branch>[-HHMM]`——
> 日期在最前（便于按时间排序）、带 `case`（现在 case00 / case01 并存）、带 `engine`（`old` / `mavis`）。
> 本文件下面的 id **一律是修订后的终稿**；若平台侧此前从任何草稿里见过
> `demo-A-mavis` 一类的旧 id，以本文件为准（旧→新映射见下表）。
> 顺带一个好处：新名里直接带分支，A/B/C 两条运行方式的记录一眼对上，不再像 `demo-2` ↔ `demo-A-mavis` 那样需要查表。
> 面向：平台侧（Governance Platform / HCI）；中英沟通兜底：需求方
> 配套：`Governance平台对接说明.md`、`case01_api.openapi.yaml`

## 一、换样本时间点

- **即日起**，`case01/runs/` 下新增三条 **mavis 新架构** 联调样本：
  `260917-demo-case01-mavis-A`、`260917-demo-case01-mavis-B`、`260917-demo-case01-mavis-C`，
  均可经只读服务读取。
- 旧样本（对接确认单冻结点 `d41cdec` 旧运行方式产物，**保留不删**，供对照）：
  `260905-demo-case01-old-A`、`260905-demo-case01-old-B`、`260905-demo-case01-old-C`。
  平台新建审核任务请优先使用三条新样本。
- 新旧样本映射关系（Branch 对齐；名字里就带分支，不必再查表）：

| 新样本（mavis） | Branch | 行为 | 对应旧样本（old 运行方式） |
| --- | --- | --- | --- |
| `260917-demo-case01-mavis-A` | A（积极买入线，已实现） | T0 满仓买入 → 09-07 退出，亏损 | `260905-demo-case01-old-A` |
| `260917-demo-case01-mavis-B` | B（建议回避线，已实现） | 不买，错失 25 万门槛合投 | `260905-demo-case01-old-B` |
| `260917-demo-case01-mavis-C` | C（条件化等待线，已实现） | T0 条件化方案 + 条件监测齐备 | `260905-demo-case01-old-C` |

> 命名对照（若需回溯旧文档）：旧 `demo-2 / demo-1 / demo-3` 依次对应
> `260905-demo-case01-old-A / -B / -C`；旧 `demo-A-mavis / -B-mavis / -C-mavis`
> 依次对应 `260917-demo-case01-mavis-A / -B / -C`。

读取方式不变：`GET /api/runs` 会发现新 run_id，`/api/runs/{run_id}`
建 Task，`/api/runs/{run_id}/full-context` 供专家查看。
服务地址仍为 `http://127.0.0.1:5002`（`case01/serve.py` 契约不变）。

### 一.1 嵌入地址变更（2026-09-19，**请平台侧替换**）

实现侧把"实时小镇"与"成品记录"合成了**一个界面**，原来给平台侧的
`http://<host>:5004/combined`（两窗一页）**已退役**，请改为：

| 用途 | 新地址 |
| --- | --- |
| 界面（小镇 + 右栏"结果记录"卡片，整页可 iframe） | `http://<host>:5010/` |
| 只嵌结果记录（压缩版式） | `http://<host>:5010/embed/review` |
| 只嵌实时小镇场景（无浮动面板、无结果卡片） | `http://<host>:5010/embed/scene` |

- 结果面板的深链参数不变：`?run=<run_id>&tab=<页签 id>`，页签 id 仍是
  `overview / turns / retrievals / events / states / reflection / router / injector / audit`。
- `http://<host>:5004/*` 不再提供服务（机器上已停），若平台侧配置里还写着它，会连不上。
- 5010 由实现侧运维起停；平台侧只负责引地址。若平台需要"只看结果、不看推演"，
  实现侧可以用 `--review-only` 模式起这个界面（不跑模拟）。

## 二、/viewer 说明

- `case01/serve.py` 存在一个 `/viewer` 端点（挂在 5002 服务上），用于实现侧
  调试/展示场景；它**不是** 平台侧 `Expert Review / Full Context` 的对接面。
- 三项只读端点（`/api/runs`、`/api/runs/{run_id}`、
  `/api/runs/{run_id}/full-context`）才是平台对接的正式契约，文档已在
  `Governance平台对接说明.md` 冻结，本次不变。
- 已知事项：`/viewer` 的可访问路径/展示范围在契约文档中**此前未明确记录**
  （阶段性缺口），本次一并说明以对齐口径——平台按 `Governance平台对接说明.md`
  的端点为准即可，无需依赖 `/viewer`；若平台曾在 `/viewer` 做过对接或
  Demo 演示，请告知实现侧以确认是否需要在契约文档补充其描述。

## 三、模型身份字段说明

- **现状**：`run.json` 当前**未记录**模型身份（后端 / 权重 / dtype / device）。
  mavis 路径默认走本地 Ollama（`qwen3:4b-instruct-2507-q4_K_M`），Ethan 可经
  环境变量走外部 API；本地 HF 权重路径（实现侧 提交）亦可能存在。
- **影响**：不同后端/权重产生的 run 在记录上**暂不可区分**。若平台在聚合
  展示时需要对 run 按"模型/后端"做筛选或标注，该信息当前缺失。
- **说明后的建议**：本阶段（阶段 0）团队已决定**不做**该组修复（记录补模型
  身份字段）。平台侧若临时需要区分，`run_id` 命名（如 `260905-demo-case01-old-B` 与
  `260917-demo-case01-mavis-B`，运行方式与日期都在名字里）可作为轻量替代；长期若要正式区分，
  请与实现侧同步排期。

## 四、需平台侧回执确认的点

1. 三条新样本是否按 `260917-demo-case01-mavis-A → -B → -C` 顺序
   替换为正式联调样本？
2. `full-context` 内容口径（自然语言、无 Branch/实验元信息）是否满足
   专家端展示？有无字段/段落需调整？
3. `/viewer` 是否曾被平台引用（用于展示规划）？若有，请告知以便补充契约。
4. 模型身份字段缺失对平台建单/聚合是否有实际影响？
5. **嵌入地址是否已从 `5004/combined` 换成 `5010`（见 §一.1）？** 若平台侧还有别处
   硬编码了 5004，请一并列出。

## 附：新样本联调数据（实现侧实测）

> 本附录数字均以 `case01/runs/260917-demo-case01-mavis-A/run.json`、`-B/run.json`、
> `-C/run.json` 的落盘记录为准（2026-09-18 核对；2026-09-19 仅改名，记录内容未变）；
> reflection 字数为该文件 `reflection.text` 的字符数，Router 问题数为 `router.issues` 条数。
> 发出前若记录有变动请重新对齐。

- `260917-demo-case01-mavis-A`：reflection 完整（5954 字），Router 拆出 4 个问题；
  T0 满仓 @45.20、09-07 退出 @27.40，现金约 125,177 元 → 符合 A 线亏损叙事。
- `260917-demo-case01-mavis-B`：reflection 完整（5096 字），Router 拆出 4 个问题；不买、现金 20 万
  不变，错失门槛合投 → 符合 B 线叙事。
- `260917-demo-case01-mavis-C`：reflection 完整（5423 字），Router 拆出 5 个问题；T0 解析出条件化
  方案（`action=wait`、条件满足后投入 20%、keyword 触发）；逐节点 `condition_monitor`
  全程 `fired=false`（Timeline A 否定句不触发）→ 未建仓、现金 20 万不变 → 符合 C 线
  "订单落空→谨慎不买"叙事。